# PCB 3D Portfolio — Technical Architecture, Microarchitecture Blueprint & Developer Transfer Log

Hi! This file is the primary reference prompt and technical transfer guide for any AI coding assistant (Claude / Antigravity / Gemini) working on this repository.

---

## 🚦 NEXT SESSION — RESUME HERE (2026-08: subsystem-identity transformation)

**Status: The portfolio reads as one board of subsystems. Board identity encodes the operator's birth date (1 Apr 2007): serial `PARAMA-2007-0401`, FW `2007.0401` (committed `819fcaa`), and every board name is now standardized on `PARAMA` (per the operator: Parameshwaran S / Parama) — `PARAMA-CORE` CPU, `PARAMA-MCU-2026-REV1` lot code, `PARAMA-DEV-BOARD` boot/dev-notes line, and the hidden cheat sequence is now `parama` (5 chars). Easter eggs complete per the brief: hidden terminal (Ctrl+K palette), boot log, serial/FW/voltage/temp (SYSTEM TELEMETRY), debug mode (DEBUG OVERLAY), hidden keyboard shortcuts (`T`/`D`), power-up boot beep, secret developer notes (`parama` chip). NEW FIX: the nav-glide hijack — `scrollToSection` now claims `glideActive` for its tween and drops wheel input while a nav glide is in flight, so clicking `[MODULES]` + a stray wheel tick (trackpad momentum) no longer redirects the page to About/Skills; verified in headless Chrome (click + mid-glide wheel tick now stays on projects). Uncommitted: easter-egg pass + PARAMA pass + nav-glide fix. Verified: typecheck + build + 12,000-frame smoke green (159 raycast poses, 32 components — AQD1 aimable).**

### Shipped in recent sessions (commit order, newest first)
- `2026-08` — **Probe-contact hover layer (uncommitted)**: the board now reads as a live instrument — every hover is a contact EVENT, not a state change. ① 3D (`hover.js`): hover-enter fires a **sonar ping ring** (one shared scene-child mesh, `RingGeometry` DoubleSide, positioned at the component's world pos +0.35z, tinted the component's glow color, scale 0.25→1.05 + opacity 0.75→0 over 0.5s power3.out, auto-hides onComplete — gated on `motionPrefs.reduced`; scene child like `hoverLight`, so the board-graph smoke audit ignores it) + the scale tap became a **springy back.out(3)** from 1.02 → 1.05 (same rest pose, so reset/exit unchanged). ② Cursor (`cursor.js` + `index.html` + `style.css`): a one-shot gold **contact-lock ring** (`.probe-lock`, `lock-ping` 0.42s keyframe) fires at the probe tip when it locks onto a board component (restarted via class-reflow on state change only — never in the hot path; hidden under reduced motion). ③ DOM (`scroll.css`): **nav route line** (gold trace draws in under `.hud-nav .nav-btn` on hover via ::before, origin-left scaleX) + ref glyph silkscreen glow; **project-card power-up** (one-shot `module-sweep` gold beam across the card, `led-respond` pulse on the status LED — briefly overriding the in-build flicker so it "powers up" the same way; ::after was free on .proj-ds); **skill-pill power-up** (silver end-cap terminations glow gold, part-role `em` lights signal-green); **CTA uplink beam** (light streak shoots across the gold pad on hover, ::after free); **scope-chip lock ping** (one-shot `scope-lock` ring pulse when `hud-scope-live` enters). Motion keyframes gated behind `(hover: hover) and (pointer: fine) and (prefers-reduced-motion: no-preference)`; static color/shadow feedback follows the plan-004 posture (hover-gated only, never stripped by reduced-motion). **Verified**: typecheck + build + 12,000-frame smoke green (raycast layer 159/159 intact); headless Chrome CDP probe — hover locks FR1, probe-target + scope-live + lock-ping all fire, ping mid-tween opacity 0.48 → auto-hides after 0.5s, zero console errors.
- `2026-08` — **Nav-glide hijack fix + PARAMA standardization (uncommitted)**: ① `src/scroll/journey.js` `scrollToSection` now sets `glideActive = true` + a new `navGlideActive` flag for its own 1.2s tween (cleared in `onComplete`, then `pumpGlide()` to chain any arrow-queued step) and `onJourneyWheel` drops wheel input (preventDefault + return) while `navGlideActive` — the bug: `scrollToSection` never claimed `glideActive`, so a wheel/key event mid-glide hit `pumpGlide()` with the flag false and started a second `gsap` tween whose `overwrite:'auto'` killed the nav tween, redirecting the page to a neighbor section (clicking `[MODULES]` landed on About or Skills). Reproduced in headless Chrome via CDP (click + one 130px wheel tick at 350ms mid-glide → landed About at y=899; after fix → projects y=1798 held 6s). Wheel-burst chaining (cap 3) unchanged. ② Board name standardized on `PARAMA`: `PARAM-CORE` → `PARAMA-CORE` (index.html About panel + portfolio.js bio), lot code `PARAM-MCU-2026-REV1` → `PARAMA-MCU-2026-REV1` (board.js ×2), dev-notes `PARA-DEV-BOARD` → `PARAMA-DEV-BOARD`, hidden cheat `param` → `parama` (main.js CHEAT + comments, scroll.css comment). Serial/boot line already said PARAMA.
- `2026-08` — **Easter-egg pass (uncommitted)**: ① hidden keyboard shortcuts — `T` toggles the SYSTEM TELEMETRY chip, `D` toggles the DEBUG OVERLAY (both free while the probe isn't flying; full-journey only), and typing the secret sequence `param` fires a one-shot gold OPERATOR NOTES chip (`#dev-notes`, auto-hides after 9s — the brief's "secret developer notes"). ② power-up beep (`sound.js` `powerUpBeep` — two ascending POST-style notes C5→G5) fired when the SND toggle turns sound ON: the toggle click is the user gesture that legally builds the AudioContext, so the board chirps the moment audio comes online; gated like every blip. New `showDevNotes()` in `telemetry.js` (setTimeout one-shot hide — wall-clock UI, not scene state). ③ per-module chip hover tooltip (`#chip-tip`, hover.js `showChipTip`/`hideChipTip`/`positionChipTip`): hovering a project chip on the board shows its BOM line — ref, subsystem theme with a signal-color dot (the module's own `signal` hex, `--tip-signal`), and title — following the cursor (transform-only, measured once per show, flipped at viewport edges, pointer-events:none, z-index 45 below the HUD). Chip `userData` now carries `chipRef/chipTheme/chipTitle/chipSignal`; `componentName` format unchanged so the scope readout's status parse stays intact.
- `2026-08` — **Subsystem-identity pass (committed `ef67dd6`)**: ① section→subsystem renaming (HUD nav `[CORE]/[MODULES]/[LIBRARY]/[EXPERIENCE]/[TX/RX]` with human aria-labels; panel titles `// DATASHEET: CORE PROCESSOR`, `// DATASHEET: EXPANSION MODULES`, `// DATASHEET: COMPONENT LIBRARY`, `// TRANSMISSION INTERFACE`; hash slugs unchanged). ② About copy rewritten in engineering voice (`PARAM-CORE` — three buses: ML/CV pipelines, full-stack, embedded/VLSI; tutorials = reference manuals; fitness as "auxiliary system"; spec-table rows re-themed CORE/FAB/CLK/VREF with 3.30V nominal). ③ Projects → expansion modules: each has `theme` (SENSOR GRID, VOICE PROCESSING UNIT...) + `signal` hex (tints the 3D chip LED + trace emissive in project-chips.js, per-chip `ledBase` for filter restore) + `spec` tags on the datasheet card and focused detail panel; **AquaDot (AQD1, WATER MONITORING MODULE, teal)** added; hero badges pruned to VREF + MODULES (9) — hackathon/cert counts removed per brief. ④ Skills → component banks: group labels C1 AI ACCELERATOR BANK / C2 DISPLAY & I/O BANK / C3 STORAGE CONTROLLER BANK / C4 FIRMWARE & RF MODULES, each pill carries its part class (`skillRoles` in portfolio.js → `MCU`, `AI ACCEL`, `COMM BUS`...). ⑤ Oscilloscope: per-module waveforms keyed by project ref (FR1 rank-ladder staircase, CP1 radar ping, DL1 audio band, BT1 packet train, AQD1 water ripple, etc.). ⑥ Easter eggs: `src/ui/telemetry.js` — SYSTEM TELEMETRY (board serial `PARAMA-2007-0401`, FW 2007.0401, rail 3.30V ± wobble, die temp 43.5°C ± wobble, uptime from the elapsed clock — fully deterministic) + DEBUG OVERLAY (FPS/frame/T+), revealed only via the palette's two secret commands; the serial/FW encode the operator's birth date (1 April 2007 — serial `PARAMA-YYYY-MMDD`, date-based FW build); contact footer carries SN + FW; contact copy is now TRANSMISSION INTERFACE with PRIMARY UPLINK CTA.
- `2026-08` — **Six-feature upgrades pass (committed `a9af7f2`)**: ① live oscilloscope CRT waveform HUD (`src/ui/oscilloscope.js`, `#hud-osc` canvas in `.hud-scope`, per-component waveforms — Y1 sine+jitter, ANT1 RF bursts, TP1/TP2 rails, U1/U2 square clocks, ECG default; scanlines drawn in-canvas since `::before` can't render on canvas). ② ENIG gold plated vias + copper pour (`board.js` `createViaArray`/`drawCopperPour`, ~40 via rings clustered at U1/U2/J1/C1–C4, lot-code silkscreen `PARAM-MCU-2026-REV1`). ③ active trace routing energize (`traces.js` `energizeTraceForSection` — surge pulse 2.5→base over 0.8s `power3.out`, ambient pulse 3× for 1.5s; wired into `journey.js` `setActivePanel`, de-energize on leave, hero no-ops). ④ BIOS terminal command palette (Ctrl+K / Cmd+K / `[CMD]` HUD button; `src/ui/command-palette.js`, phosphor-green terminal, fuzzy search over sections/utilities/links, ↑↓/Enter/Esc). ⑤ tactile relay click + switch clack + scroll-velocity electrical hum (`sound.js` — all gated behind SND toggle, AudioContext never built outside a user gesture; `power.js` `togglePower` throws the relay; `handleSectionKey` clacks; passive scroll handler drives the hum). ⑥ SMD DIP-switch project filter (`sections.js` `.proj-filter-bar` ALL/AI-ML/FULL-STACK/SYSTEMS — cards animate + `setProjectFilter` syncs the 3D chips: dimmed emissiveIntensity 0.05, LEDs pinned off).
- `2026-08` — **Living-Board pass (plans 014 + 015)**: bench sweep laser proximity reaction on CPU U1 silicon die (`opacity 0.8 -> 0.95`), night-bench calmer float modulation (`0.65x speed, 0.75x amp`), `.nav-active` gold underline opacity transition, full `motionPrefs.reduced` gating.
- `25ca4fe` — **keyboard section-stepping**: ArrowUp/Down and PageUp/Down glide one section each with shared 1.2s `power2.inOut` queue.
- `ffd2369` — **CI validation trio**: typecheck + build + 12,000-frame smoke test on push/PR.
- `fa6e363` — **daughterboard card redesign + full-board framing (z=23) + fab-bench backdrop + shadow grounding + board-first hero + dead-symbol sweep + checkJs on journey/boot**.
- `d7b8f52` — `public/og-preview.png` (1200×630 social share card) via the `.freebuff/og-tools` headless pipeline + `?og=1` instant-boot capture mode.

### Open items (nice-to-haves, no urgency)
- **~~Execute all plans 001–007, 011–015~~ ALL DONE** — all 12 motion & living-board plans executed and verified.
- **~~Full checkJs coverage~~ DONE** — `// @ts-check` covers all 12 modules.

---

## 🎯 MANDATORY PRIMARY PROMPT & EXECUTION DIRECTIVE

> **IMPORTANT**: The following prompt defines the exact architectural pattern and phased implementation workflow for all 3D PCB component interactions in this portfolio. You MUST follow this blueprint strictly.

```text
"Build and verify ONE interaction before touching anything else: clicking a single labeled component on the PCB board (start with the About/education component) must do the following, in order:

1. Raycast against pointer click position to detect which mesh was clicked — use THREE.Raycaster, not a DOM click handler on an invisible div layered over the canvas.
2. On hit, animate camera.position and the camera's lookAt target from current values toward that mesh's position using GSAP (duration: 1.2, ease: 'power2.inOut'). The camera must visibly travel through 3D space — no cuts, no instant snaps.
3. Once the tween completes, reveal an info panel anchored to the mesh's projected 2D screen position (mesh.position.clone().project(camera), converted to screen coordinates, updated every frame) — so the panel visually tracks the component instead of floating independently.
4. Add a close control (X, or click elsewhere) that reverses the camera tween back to the prior position and hides the panel.

Do not build, touch, or 'improve' any other section until this exact loop works and I've confirmed it live in the browser myself. Once I confirm it, apply the identical pattern — raycast → camera tween → anchored panel → reverse tween — to every other labeled component (experience, skills, projects, certs), swapping only the content.

Also fix the duplicate 'Connect on LinkedIn' button currently stacked in Contact — there should be exactly one per section, no leftover elements."
```

---

## 🧭 Current Interaction Model — SINGLE scroll-journey model (READ FIRST)

**The legacy click-to-zoom stack was DELETED (2026-08): `camera-states.js`, `sidepanel.js`, `tooltip.js`, `pcb-hud.js` are gone; hover.js is hover-glow only; `#pcb-tooltip` removed from `index.html`; all related CSS removed.** There is now ONE interaction model, with a degraded static-scroll mode for accessibility:

| Mode | Body Class | Who Gets It | Camera / Interaction |
|:---|:---|:---|:---|
| **Scroll-Journey** | `full-journey` | Default (viewport ≥ 768px AND no reduced-motion) | Camera flies along a CatmullRomCurve3 path driven by GSAP ScrollTrigger scrub (`setCameraAtT(t)`). Panels are fixed overlays side-anchored to the projected component position; activation is a pure function of the current scroll leg. |
| **Lite** | `lite-mode` | `prefers-reduced-motion: reduce` OR viewport < 768px (`isLiteMode()` in `src/config.js`) | No scroll-jacking; sections stack normally (all content reachable by scrolling). Hover glow still works on the fixed canvas. No zoom, no tooltip, no sidepanel. |

**Consequences you must respect in code:**

1. `setCameraAtT(t)` owns the camera every frame in journey mode — do NOT reintroduce any camera LERP/zoom.
2. `initJourney(camera)` runs **after** the boot sequence completes (main.js boot callback) — the `boardGroup` param was removed in the 2026-08 dead-symbol sweep; only `updateJourneyEffects(camera, boardGroup)` still receives the group. Before `initJourney`, `journeyReady` is false and `updateJourneyEffects` no-ops — this gate exists so boot's GSAP inline styles on the hero panel are never fought mid-boot.
3. Panel activation is leg-derived (`setLegState` in ScrollTrigger `onUpdate`, switch at ≥0.55 progress) — no distance scanning, no cooldowns. See session log.
4. `#hud-bar` interactivity is gated by the `.hud-ready` class — see the Critical Gotchas section.
5. `main.js` step 14 binds nav buttons to `scrollToSection()` only.

---

## 🛠️ Core Tech Stack & Tooling

- **Vite & Vanilla JavaScript (ES Modules)**: Fast development server and production bundler without framework overhead.
- **Three.js (WebGL)**: Custom 3D printed circuit board substrate, SMD IC component meshes, trace copper pathways, cylinder vias, project chips, and glowing electron flow particles.
- **GSAP (GreenSock Animation Platform)**: Powers camera 3D space travel (`duration: 1.2`, `ease: 'power2.inOut'`), boot scanline sequence, typewriter terminal logs, and panel transitions.
- **Pure Vanilla CSS**: SCANLINE CRT overlay filters, CSS custom property design system tokens, responsive glassmorphism HUD cards.
- **Typography**: `Chakra Petch` (HUD headings, component IDs) + `Fragment Mono` (Data registers, spec tables, terminal) + `Instrument Sans` (body prose).

---

## 📁 Repository Directory Blueprint & Responsibilities

```
c:\Users\hunte\Parameshwaran_S_Portfolio\
├── main.js                  # Entry point: scene initialization, tick callbacks, boot execution, event listeners
├── index.html               # Semantic HTML structure, CRT filters, HUD header, section datasheet templates
├── style.css                # Primary design system: CSS tokens, scanlines, glassmorphism panels, CTA button styles
├── scroll.css               # Scroll journey overrides: camera path container, screen-space panel positioning, connector SVG
├── claude.md                # Master developer transfer log & prompt blueprint (this file)
└── src/
    ├── config.js            # External URLs (LinkedIn, GitHub), responsive breakpoints (768px lite mode), env helpers
    ├── data/
    │   └── portfolio.js     # Data source of truth: CV metrics, project list with status ('shipped' | 'building'), timeline, skills
    ├── three/
    │   ├── scene.js         # PerspectiveCamera, lighting setup, renderer config, bloom pass, FPS performance guardrail
    │   ├── board.js         # Board substrate extrusion, soldermask plane, CanvasTexture silkscreen, micro-tilt parallax
    │   ├── components.js    # 3D SMD components (U1, U2, Y1, ANT1, J1, VR1, RN1, D1-D7), silicon die + radar ring, interaction flags
    │   ├── traces.js        # Solid 3D copper trace segments (BoxGeometry), corner vias (Cylinders), trace pathways data (TraceRoute typedef)
    │   ├── particles.js     # Electron flow particles along trace paths (constant speed — hover speed boost removed)
    │   └── project-chips.js # Data-driven project chips (soldered vs breadboard jumpers based on portfolioData.projects.status)
    ├── scroll/
    │   └── journey.js       # CatmullRomCurve3 camera path, GSAP ScrollTrigger legs, per-frame Vector3.project screen positioning
    ├── ui/
    │   ├── boot.js          # Retro terminal boot sequence: laser scanline, typewriter status logs, badge pop-ins
    │   ├── sections.js      # Datasheet HTML injection from portfolio.js data, profile link wiring
    │   └── fallback.js      # WebGL detection & non-WebGL fallback screen
    └── utils/
        └── hover.js         # THREE.Raycaster hover-glow ONLY: bounded pointer clamp + targetMouse inertia + parallax feed. No camera control, no click-zoom.
```

---

## 🔬 Component Coordinate & Dimension Specification Table

| Ref Identifier | Component Description | 3D Mesh Geometry | Board Position (x, y, z) | Camera Target Pos (x, y, z) | Camera LookAt (x, y, z) |
|:---|:---|:---|:---|:---|:---|
| **U1** | Main CPU (About / Education) | Box 2.4 × 2.4 × 0.22 | `(0, 1.0, 0.085)` | `(0, 1.0, 2.0)` | `(0, 1.0, 0.085)` |
| **U2** | GPU (Project Array) | Box 1.8 × 1.8 × 0.18 | `(-3.2, 4.5, 0.085)` | `(-3.2, 4.5, 2.0)` | `(-3.2, 4.5, 0.085)` |
| **C1–C4** | Decoupling Capacitor Bank (Skills) | Cylinders R=0.2, H=0.7 | `(3.2, 4.5, 0.085)` | `(3.2, 4.5, 2.0)` | `(3.2, 4.5, 0.085)` |
| **Y1** | Crystal Oscillator (Education detail) | Box 1.2 × 0.6 × 0.26 | `(-3.5, 0.5, 0.115)` | `(-3.5, 0.5, 1.8)` | `(-3.5, 0.5, 0.085)` |
| **ANT1** | RF Antenna (Contact TX/RX) | Box 1.0 × 1.0 × 0.15 | `(3.5, 0.5, 0.135)` | `(3.5, 0.5, 1.8)` | `(3.5, 0.5, 0.085)` |
| **J1** | USB-C Power Bus (Experience) | Box 1.2 × 0.8 × 0.32 | `(0, -7.3, 0.145)` | `(0, -7.3, 2.0)` | `(0, -7.3, 0.085)` |
| **VR1** | Voltage Regulator (Tech Stack) | Box 0.7 × 0.7 × 0.16 | `(3.5, -4.5, 0.085)` | `(3.5, -4.5, 2.0)` | `(3.5, -4.5, 0.085)` |
| **D1–D7** | Status LED Array | Bounds 2.4 × 1.4 | `(-3.5, -4.5, 0.085)` | `(-3.5, -4.5, 2.4)` | `(-3.5, -4.5, 0.085)` |

*Board substrate dimensions: width = 11.0, height = 15.0, thickness = 0.16, top surface Z = 0.085.*

---

## ⚡ The 4-Step Raycast → Camera Tween → Anchored Panel System

*(⚠️ HISTORICAL — the legacy click-to-zoom stack described below was DELETED in 2026-08 (`camera-states.js`, `sidepanel.js`, `tooltip.js`, `pcb-hud.js` removed; hover.js is glow-only). Kept as the original build blueprint. The shipped interaction is the scroll journey: `setCameraAtT(t)` + leg-derived panel activation + screen-anchored panels in `src/scroll/journey.js`. The only parts still live: the `THREE.Raycaster` hover-glow pattern in `src/utils/hover.js` and the screen-projection anchoring math in `updateJourneyEffects()`.)*

### Step 1: Pointer Click Raycasting (`THREE.Raycaster`)
- **Module**: `src/utils/hover.js`
- **Logic**: A click event listener captures mouse pointer coordinates (`mouse.x`, `mouse.y` normalized device coordinates between -1 and +1).
- **Pointer Motion Engine**: Implements bounded `clamp` boundary calculations (NDC ±1) and 500ms smooth inertia tracking. `targetMouse` is the instant, clamped pointer position (updated by `mousemove`/`touch`); `mouse` lerps toward it every frame (`mouse.x += (targetMouse.x - mouse.x) * 0.08`) and feeds only the board parallax tilt:
  ```javascript
  const clamp = (val, min, max) => Math.min(Math.max(val, min), max);

  const updateMouseCoords = (clientX, clientY) => {
      const hw = window.innerWidth / 2;
      const hh = window.innerHeight / 2;
      targetMouse.x = clamp((clientX - hw) / hw, -1.0, 1.0);
      targetMouse.y = clamp(-(clientY - hh) / hh, -1.0, 1.0);
  };
  ```
- `THREE.Raycaster.setFromCamera(targetMouse, camera)` computes the ray from the camera lens through the pointer. **Raycasts use `targetMouse` (instant, accurate); the smoothed `mouse` vector is used only for parallax tilt via `updateBoardParallax()`.**
- Hit testing targets `interactiveObjects` (meshes with `userData.isInteractive = true`). DOM click handlers on overlay divs are strictly forbidden.

### Step 2: Visible 3D Camera Travel (`GSAP Tween`)
- **Module**: `src/utils/camera-states.js` & `src/utils/hover.js`
- **Logic**: On hit detection, GSAP animates both `camera.position` and `currentLookAt` vector over 1.2 seconds:
  ```javascript
  gsap.to(camera.position, {
      x: targetPos.x,
      y: targetPos.y,
      z: targetPos.z,
      duration: 1.2,
      ease: 'power2.inOut'
  });
  gsap.to(currentLookAt, {
      x: targetLook.x,
      y: targetLook.y,
      z: targetLook.z,
      duration: 1.2,
      ease: 'power2.inOut',
      onUpdate: () => camera.lookAt(currentLookAt),
      onComplete: () => revealScreenAnchoredPanel(ref)
  });
  ```
- No instant jumps or camera position cuts. The camera visibly glides through 3D space.

### Step 3: Screen-Space 2D Anchored Panel Positioning
- **Module**: `src/scroll/journey.js` & `src/ui/pcb-hud.js`
- **Logic**: During the render loop (every tick), the 3D world position of the focused component is projected into 2D viewport pixel coordinates:
  ```javascript
  const worldPos = new THREE.Vector3();
  componentMesh.getWorldPosition(worldPos);
  
  // Project vector into normalized device coordinates (-1 to +1)
  const ndc = worldPos.clone().project(camera);
  
  // Convert NDC to screen pixel coordinates
  const screenX = (ndc.x * 0.5 + 0.5) * window.innerWidth;
  const screenY = (-ndc.y * 0.5 + 0.5) * window.innerHeight;
  
  // Update anchored panel CSS position
  panelElement.style.left = `${screenX + offsetX}px`;
  panelElement.style.top = `${screenY + offsetY}px`;
  ```
- An SVG connector line (`<svg id="connector-overlay"><line ... /></svg>`) connects the component center to the screen card edge in real time.

### Step 4: Reverse Camera Tween & Panel Dismissal
- **Logic**: Clicking the close control (`[X]`), hitting the `Escape` key, or clicking empty board space triggers `reverseCameraTween()`:
  - Hides the active screen-space datasheet card (`opacity: 0`, `pointer-events: none`).
  - Animates `camera.position` and `currentLookAt` back to their prior values (or default PCB view `(0, -2, 17)`) using GSAP (`duration: 1.2`, `ease: 'power2.inOut'`).
  - Restores board component shell visibilities and interactivity.

---

## 🎨 UI & Layout Rules: LinkedIn CTA Single-Button Constraint

### Problem & Fix Requirement
Previously, multiple `.cta-linkedin` elements were present simultaneously in the Contact section (one in `#hud-bar` header and one in `#panel-contact`), causing stacked duplicate buttons.

### Execution Rule
1. There MUST be **exactly ONE LinkedIn button visible per section**.
2. **Data-driven rule (current)**: `setActivePanel()` in `src/scroll/journey.js` toggles `body.hud-cta-hidden` whenever the active panel embeds its own `.cta-linkedin` (hero, about, contact all do). `scroll.css` hides `#cta-linkedin-hud` under that class (`opacity: 0 !important; pointer-events: none !important; visibility: hidden !important;`). `visibility: hidden` also removes the link from the tab order. *(The old `in-hero-section` / `in-contact-section` classes were removed — do not restore them.)*
3. Every LinkedIn CTA element automatically receives `href = LINKEDIN_URL` via `wireProfileLinks()` in `src/ui/sections.js`.
4. All secondary links (e.g. GitHub) must have accurate accessibility attributes (`aria-label="Visit my GitHub profile (opens in new tab)"`).

**Known remaining gap (do not "fix" without user confirmation):**
- **Lite mode**: the `hud-cta-hidden` toggle only runs in journey mode (`setActivePanel`), so lite-mode users see the HUD CTA *plus* the hero/contact panel CTA together. The HUD CTA is also the only CTA on Skills/Projects/Experience in lite mode, so a blanket hide has tradeoffs.

---

## 🔍 Verification & Testing Protocols

After making any code changes, verify your work using these exact steps:

```bash
# 1. Run development server and verify live in browser
npm run dev

# 2. Run production build check (must exit with code 0)
npm run build

# 3. Check for TypeScript / linting issues — npm run typecheck (tsc --noEmit)
#    No .ts files in src. Validates the checkJs-opted files (opt-in via "// @ts-check"
#    at the top of each; tsconfig sets "allowJs": true, "checkJs": false):
#      - src/scroll/journey.js  (camera path, leg state, panel positioning)
#      - src/ui/boot.js         (deterministic boot choreography)
#      - src/utils/hover.js     (raycast hover-glow + pointer inertia)
#      - src/three/scene.js     (renderer, lights, backdrop, shadow catcher, tick loop)
#      - src/three/traces.js    (copper trace routes — TraceRoute typedef feeds journey/boot/particles)
#      - src/three/board.js     (substrate, silkscreen canvas, mounting holes, parallax)
#      - src/three/components.js (SMD components — interactiveObjects typed THREE.Mesh[])
#      - src/three/particles.js (electron flow — Particle typedef)
#      - src/three/project-chips.js (project chips — FlickerLed typedef)
#      - src/config.js          (URLs, breakpoints, env helpers)
#      - src/ui/sections.js     (datasheet injection, profile link wiring)
#      - src/ui/fallback.js     (WebGL detection, cleanup)
#    Keep their JSDoc types accurate when refactoring so the check stays green.
npm run typecheck
```

### Manual Browser Checklist
*(The legacy click-to-zoom checklist is obsolete — the zoom flow was deleted. Journey-mode checks below.)*
1. Check the **Contact section** — confirm there is **exactly ONE 'Connect on LinkedIn' button** visible (no duplicate stacking).

### Manual Browser Checklist (scroll-journey mode)
1. After boot, the **HUD nav buttons are clickable** — `getComputedStyle(hudBar).pointerEvents` must be `auto` (the `!important` on `.hud-ready` beats boot's inline `pointer-events: none`).
2. Click `[PROJECTS]` — the camera travels and the **wide projects panel stays fully on-screen** (no overflow past the right edge).
3. Navigate to any non-hero section — the **hero panel is hidden** (no leftover hero CTA / scroll-hint overlaying other sections).
4. At hero and contact — exactly one LinkedIn CTA visible, and the hidden HUD CTA must be **absent from the tab order** (Tab through the page; `visibility: hidden` removes it).

---

## ⚠️ Critical Gotchas — Fixed Defects That WILL Regress (keep these invariants)

The following were found by adversarial review (2026-08) and fixed. Any future refactor must preserve these invariants:

1. **Never leave inline styles that override class-driven visibility toggles.**
   - `#hud-bar.hud-ready` **must** keep `pointer-events: auto !important` in `scroll.css`. `boot.js` writes an inline `pointer-events: none` via `gsap.set()` and never clears it; without `!important`, the fixed HUD (nav, brand link, LinkedIn CTA) is **permanently unclickable** in full-journey mode.
   - `boot.js` hero reveal must keep `clearProps: 'opacity,visibility'` on its `tl.set(heroPanel, ...)`. Without it, the inline styles permanently override `body.full-journey .ds-panel` / `.panel-active`, leaving the hero panel stuck on top of every other section.
2. **Panel anchoring must use the real rendered panel width**: `const panelW = panel.offsetWidth || Math.min(480, window.innerWidth - 40)` in `updateJourneyEffects()`. `#panel-projects` is `.ds-panel-wide` (up to 980px); a hardcoded 480 shoves it off-screen (measured at `left: -321px`).
3. **`buildCurves()` resets `stopOrder`, `stopTs`, `stopPosVectors`** at the top so re-initialization (HMR / re-entry) can't duplicate stops or leave stale t-mappings.
4. **Silicon die pulse is finite** (`repeat: 7` + `onComplete` settle at opacity 0.65) — do not restore an infinite `repeat: -1` pulse.

---

## 📝 Developer Change & Session Log

- **2026-08 Session — discrete-text-sequence applied to the boot terminal lines**: the first boot line (`> INITIALIZING PARAMA-DEV-BOARD...`) no longer types uniformly — it now runs the same discrete-text-sequence pattern as the subtitle. `boot.js` `BOOT_LINES` became `Array<string | BootLine>` with a `BootLine` typedef (`{ text, sequence?: {t, text}[], total? }`); the first entry carries a deterministic typo schedule: `> INI` → `> INITI` → **`> INITILIZING`** (typo — dropped 'A' in INITIALIZING) → `> INITI` (backspace to the fork) → corrected bulk paste `> INITIALIZING PARAMA-DEV-BOARD...`. The typing loop branches: plain-string lines keep the uniform proxy tween; sequenced lines get one `ease:'none'` driver tween whose onUpdate reverse-searches the sparse schedule (identical mechanics to the subtitle), `total: 1.4` (the typo drama needs a beat longer than the original 1.02s uniform type — shifts the live-status re-append from ~4.0s to ~4.45s, still well before the 5.85s statusFinal update), plus a final-state `tl.call` snapping the corrected line exact. All other lines + the status register are untouched. **Verified live**: re-ran the boot in-page (cleared the `psb-booted` skip flag, restored the overlay, called `runBootSequence()` directly) and sampled the first line — the exact sequence rendered: `> INITI` → `> INITILIZING` → `> INITI` → `> INITIALIZING PARAMA-DEV-BOARD...`; final terminal state has all three lines exact, status register reaches "ALL SYSTEMS OPERATIONAL", console clean. `npm run typecheck` + build green. (Note: `preview_navigate` reloads swallow the boot window — the tool waits ~15s for a load event that never arrives mid-boot; re-run the boot in-page to observe it.)
- **2026-08 Session — gradient-text-sweep extended to every datasheet title**: the hero-name twin mechanism (`.headline-stack` + `.headline-twin`, `background-clip: text`) now applies to each panel's `h2.ds-title` — `index.html` wraps all five static titles (ABOUT/PROJECTS/SKILLS/EXPERIENCE/ALL PCB SYSTEMS OPERATIONAL) plus the focused-project `#pdetail-title` in `.headline-stack.ds-title-stack` grids with pixel-identical twin spans (`aria-hidden`). `scroll.css` adds the metric-matching overrides — `.ds-title-stack .headline-twin` gets the h2's exact `padding-left: 12px` (tick offset), `22px / 3px / 700 / var(--font-heading)`, a 640px-media `16px` twin match for the mobile `.ds-title` shrink, and `.contact-panel .ds-title-stack .headline-twin { padding-left: 0 }` (centered panels drop the tick). `journey.js` `setActivePanel` now fires a one-shot sweep on activation of any non-hero panel (reuses the boot's exact tween language — `backgroundPosition 100%→0%` at `ease:'none'`, raised via `gsap.set(opacity:1)`, faded back at 1.2s with `clearProps: 'backgroundPosition'`); duration shortened 1.6→1.2s because a narrow 22px title swept at hero's duration reads draggy (travel is proportional to element width). **Hero panel excluded on purpose** — boot.js owns its twin; journey's `setActivePanel('panel-hero')` at init would otherwise double-sweep right after boot. `fillProjectDetailPanel` fills `#pdetail-title-twin` with the same text as the title. Verified: `npm run typecheck` + build green; live — twin metrics match the h2 exactly (font/weight/spacing/family/padding/text, resting opacity 0), focus-activation caught the sweep mid-travel (`backgroundPosition 78.8% 50%`, opacity 1 → back to 0), contact panel raises its twin, hero twin stays 0. Reduced-motion users never see it (lite mode = no journey).
- **2026-08 Session — social-card metadata made absolute (bulletproof sharing)**: `index.html` head now uses `https://imnotparama.github.io/Parameshwaran_S_Portfolio/` as the single **SITE_BASE** (loud comment marks it as the one place to swap for a custom domain — GitHub Pages is the deploy target; Pages is currently disabled on the repo, so the URL isn't live yet). Everything derives from it: `og:url`, `og:image` (+ `og:image:secure_url`), `twitter:image` (was missing entirely), `<link rel="canonical">` (was missing — Google uses it for dedupe), and the JSON-LD `image` (was relative `/og-preview.png`). Relative og:image URLs get dropped or mis-cached by LinkedIn/Discord scrapers, and Google dedupes via og:url/canonical — both were the stated failure modes. Verified: `npm run build` green and the built `dist/index.html` carries all five absolute URLs; live DOM parse confirms canonical + og:url + og:image + twitter:image + JSON-LD image all resolve to the Pages base. NOTE: re-scrape LinkedIn/Discord debuggers after the site is actually deployed (they cache aggressively).
- **2026-08 Session — click-to-component resurrected in the scroll-journey model (the flagship interaction)**: project chips on the board are now raycast-clickable — clicking one glides the camera to the chip and opens a focused datasheet anchored next to it. **The interaction** (exactly the blueprint's loop, adapted to the leg-driven model): `hover.js` gained a canvas-only click listener (`setBoardClickHandler`) — raycasts the click's NDC against `interactiveObjects`, forwards `userData.type === 'PROJECT'` hits by ref (listener is on `#threejs-canvas`, so HUD/panel clicks never reach it). `project-chips.js` now exports `projectChips` — `{ ref → { pos (boardGroup LOCAL space, same language as COMPONENT_WORLD), data, ledMat } }` — with `buildSolderedChip`/`buildBreadboardPatch` returning their LED materials for the focus flash. `journey.js` added a `focusedChip` state + `focusProject(ref)` (exported): fills `#panel-project-detail` from `portfolio.js` (textContent only — ref/status line, title, problem, state, tag pills, link CTA), activates it via `setActivePanel`, flashes the chip's LED (same "if it glows, it's live" language as `pulseArrival`), and glides the camera to `chip.pos + CHIP_FOCUS_OFFSET (0, 1.5, 2.8)` (closer than section stops — the chip is 0.42u) using the extracted `glideCameraTo(pos, look, duration)` (power2.inOut, `ARRIVAL_GLIDE_DURATION` default). **Release paths (all verified live)**: any scroll — `setCameraAtT` calls `clearFocus(false)` and the scrub owns the camera again; Esc (journey keydown, ignores INPUT/TEXTAREA); the panel's `✕` button (`#btn-project-close` → `exitFocusMode()`, both glide back to the current leg's stop pose); clicking the same chip toggles off. `updateJourneyEffects` was refactored: panel anchoring extracted into `positionPanelAt(panelId, cx, cy)` (the old inline block — pre-calculated sizes, side-pick, clamp, connector draw), reused by both the COMPONENT_WORLD branch and the new focus branch (detail panel anchors to the chip's projected position every frame, connector line included). `index.html` gained the `#sec-project-detail` section — `display: contents` in scroll.css so its `min-height: 180vh` `.journey-sec` can't extend the scroll path — holding `#panel-project-detail` (`.ds-panel` with `.panel-close` gold SMD button, `.ds-ref`, `.ds-title`, `#pdetail-problem/state/tags/link`). `createConnector` is now idempotent (removes any existing `#connector-line` before appending — a dev-mode double module graph stacked two fixed overlays; harmless but sloppy). **Verified**: `npm run typecheck` + build green; live via the REAL click path (synthetic canvas click at CP1's projected screen position → focus fired through the actual handler chain: panel active + filled, camera at `(-4.20, 4.40, 2.88)` = CP1 pos + offset, connector `display: block`, HUD CTA hidden); FR1 breadboard chip reads `FR1 — BREADBOARD (IN BUILD)`; toggle/scroll/Esc/close all release correctly; console clean. Note for testers: dynamic `import('/src/...')` in the dev console resolves a SECOND module instance (empty `projectChips`, null camera) — Vite appends `?t=<mtime>` to the real graph URLs; use `performance.getEntriesByType('resource')` to find the `?t=` URLs.
- **2026-08 Session — three-adapter renderer-quality ladder (composer resolution + shadows + bloom)**: `src/three/scene.js` gained a **whole-budget quality ladder** replacing the bloom-only guardrail. Findings that drove it: (1) `EffectComposer._pixelRatio` defaults to **1** and is never synced to the renderer — the bloom path silently rendered the scene at CSS-pixel resolution on retina and upscaled (soft), while the guardrail only ever scaled bloom strength; (2) the actual fill costs in the bloom path — composer resolution, then shadow map — stayed pegged at full under load. **Fix**: new `QUALITY_LEVELS` ladder — level 0 = the tuned baseline exactly (composer ×1.0, shadows 1024², bloom 0.45/0.3), level 1 = ×0.75 / 768² / 0.2·0.15, level 2 = ×0.5 / 512² / 0.1·0.08. `applyQualityLevel()` scales composer resolution (the real fill cost) + shadow map (dispose + null to force re-render) + bloom together; `enableBloom()` now syncs the composer ratio to the renderer at init; the resize handler re-applies the current level so resolution/shadows stay in budget. Added **hysteresis** — recover only when fps climbs ~5 above the downgrade floor, so borderline machines don't thrash every 30 frames. Verified: `npm run typecheck` + build green; boot completes clean (the SwiftShader shaderSource exception is the pre-existing software-rendering artifact, not this change); in this DPR=1 environment the composer ratio computes to 1.0 (identical to before) — the retina-DPR-2 fix and the down-step ladder are the real behavioral gains, feel-checkable on hardware.
- **2026-08 Session — threejs-animation perf pass (60 per-particle PointLights removed)**: `src/three/particles.js` gave every electron (5 × 12 traces = 60) its own `PointLight(0xffffff, 0.3, 1.5)` — all switched on for the whole journey — forcing every lit material's shader to evaluate 60 point-light attenuations per fragment through bloom + shadow passes. The particles are already emissive (`emissiveIntensity: 2.5`, `0x3ee6a0`) with bloom threshold 0.7, so the "moving glow" was redundant with the glow they already emit. **Fix**: deleted the per-particle light + the 60 identical `defaultMaterial.clone()` calls (nothing differentiates them per-particle — only `mesh.visible` toggles; the clones were 60 identical program bindings). Now ONE shared material, zero particle lights; scene keeps only its two intentional PointLights (`pcbBacklight` scene.js:203, `hoverLight` hover.js:47). Verified: `npm run typecheck` + build green; grep confirms zero per-particle lights; boot completes clean with no console errors. Feel-check on real hardware still pending (preview webview not compositing frames this session) — if the electrons read too flat on a device, the fallback is one shared PointLight per trace (12, not 60) following the leading particle.
- **2026-08 Session — frontend-design cohesion pass (finish, not redesign)**: the identity (FR-4/soldermask/ENIG gold, daughterboard panels, warm IPC silkscreen) was audited and three incoherences fixed against the site's own rules. **(1) Signature move — HUD nav is now the board's pin map**: each nav button gained a tiny gold reference designator (`<span class="nav-ref" aria-hidden="true">U1</span>`, U2, C1–C4, J1, ANT1 — matching the panel silkscreen refs), styled at 9px / `var(--enig-gold)` / 0.8 opacity (1.0 on hover/active); aria-hidden so the buttons' aria-labels keep the clean names. **(2) Cold-white unification**: all `#ffffff` → `var(--silkscreen)` (#ece7d8) — `.ds-title`, `.proj-ds-title`, `.tl-title` (scroll.css), `.fallback-title` + `.nav-btn:hover` (style.css) — the page previously rendered two whites (pure cold white on titles vs the warm IPC silkscreen identity), with the hero name already on silkscreen. **(3) Motion-token completion**: the raw-ease stragglers plan 003 left off-token are now tokenized — `.proj-ds` `0.3s ease` → `var(--duration-base) var(--ease-out)`, `.proj-ds-link` + `.secondary-link` `0.2s` → `var(--duration-fast) var(--ease-out)`, `body.hud-cta-hidden #cta-linkedin-hud` `0.3s ease` → tokens. Verified: `npm run typecheck` + build green; greps clean (zero `#ffffff`, zero raw `0.3s ease`/`0.2s` in both sheets); live — refs render in ENIG gold, `.ds-title` resolves `rgb(236,231,216)`, `.proj-ds` transition computed as `0.3s cubic-bezier(0.22,1,0.36,1)`.
- **2026-08 Session — hyperframes-animation rules composed into the hero (discrete-text-sequence + gradient-text-sweep)**: the boot's subtitle typing became a **discrete text sequence** (`src/ui/boot.js`) — a sparse `{t, text}` schedule (`SEQUENCE`) with keystroke clusters, a typo (`ECE + Data Sience`), backspaces peeling to the fork (`ECE + Data S`), then a corrected bulk paste (`ECE + Data Science`), driven by ONE `ease:'none'` proxy tween with a reverse-search `textAt()` in `onUpdate` (pure function of timeline time — deterministic, no timers; the old smooth per-char slice was uniformly machine-typed). A `tl.call` at `SCHEDULE.subtitle + TOTAL` snaps the final text exact as a safety net. The hero name got a **gradient-text-sweep**: `index.html` wraps `h1#user-name` in a `.headline-stack` (CSS `display:grid`) with an `aria-hidden` `.headline-twin` span stacked `grid-area: 1/1` — `background-clip:text`, `background-size: 300% 100%`, `background-position: 100% 50%`, `color: transparent`, `opacity: 0` by default (resting design untouched). Boot raises it at `SCHEDULE.board`, tweens `backgroundPosition` 100%→0% (percent axis inverted → left→right travel) at `ease:'none'` for 1.6s (gold→green ENIG sweep masked INTO the glyphs), then fades back to 0 so the solid silkscreen h1 owns the rest state. Also per the easing doctrine: board float-up + underline draw moved `power2.out` → `power3.out` (house entrance settle — the board float is the hero entrance, power2 is for secondary motion). `style.css` gained the stack/twin rules + a 768px mobile twin override (matches the h1's smaller clamp + 0.08em spacing). Validated: `npm run typecheck` + build green; live — twin computed style confirmed (`background-clip:text`, 300% size, transparent fill, resets to opacity 0), the SEQUENCE reverse-search verified in-page at all four key states, and the sweep visually confirmed over the name (pale ENIG-gold/green hue distinct from the resting silkscreen); skip-boot path unaffected (twin stays hidden, subtitle lands on the corrected full text).
- **2026-08 Session — JSON-LD Person structured data added**: `index.html` head gained an `application/ld+json` block — `@type: Person` with `name`, `jobTitle` ("ECE + Data Science Student"), `alumniOf` (SRM Institute of Science and Technology, Ramapuram), `sameAs` (LinkedIn + GitHub, mirroring `config.js`), `image` (`/og-preview.png`), and `knowsAbout` (all 25 skills across the four `portfolio.js` groups: ai_ml/web/data/hardware). **Static in the raw HTML by design** — crawlers and recruiter scrapers read markup without executing JS (dynamic injection from portfolio.js would defeat the purpose). A comment in the block flags the sync requirement with `portfolio.js`. Validated: `JSON.parse` of the extracted block via node + in the live DOM (25 skills, 2 sameAs), `npm run build` green.
- **2026-08 Session — Plans 003 + 004 executed (motion audit COMPLETE)**:
  - **004 — hover lifts gated** behind `@media (hover: hover) and (pointer: fine)` in six places: `.hud-nav .nav-btn:hover` (scroll.css), `.cta-linkedin:hover` (scroll.css, split from `:focus-visible` — keyboard half stays ungated), `.proj-ds:hover` + `.proj-ds.is-building:hover` (merged into one gated block, current lift `-3px`), `.skill-pill:hover` (found live beyond the plan's list — its `-1px` lift would have tripped the verification grep), `.nav-btn:hover` (style.css — also removed the green box-shadow that leaked onto HUD hovers), `.cta-linkedin:hover, .cta-contact:hover` (style.css). Color/bg/border/shadow feedback, `:active` dips, and `:focus-visible` stay ungated. Every `translateY` lift now sits inside a gated block (6 media blocks).
  - **003 — motion tokens consolidated**: all six hand-typed `cubic-bezier(0.22, 1, 0.36, 1)` → `var(--ease-out)` (vignette + 5 panel/typography rules; durations preserved verbatim — the only remaining raw curve is the `:root` token definition). Deleted the "Minimalist Navbar styles" `.nav-btn` block (strict subset of Enhanced; `scroll.css` owns the HUD cascade). Tokenized Enhanced `.nav-btn` + CTA `0.25s ease` → `var(--duration-fast) var(--ease-out)` (0.18s — deliberate snappiness correction) and both `.skip-link` `0.3s ease` → `var(--duration-base) var(--ease-out)`. `.proj-ds` transitions untouched per plan scope.
  - **Verified**: build + tsc green; greps clean (no raw curve outside `:root`, no `0.25s ease`, no Minimalist block, all lifts inside media blocks); live computed styles resolve to tokens (nav/CTA `0.18s cubic-bezier(0.22,1,0.36,1)`, panels `0.5s` unchanged curve); boot → hero active, HUD clickable, console clean. `plans/README.md` status + execution log updated.
- **2026-08 Session — checkJs coverage sweep COMPLETE (all 12 modules checked)**: `// @ts-check` added to `components.js`, `particles.js`, `project-chips.js`, `board.js`, `config.js`, `sections.js`, `fallback.js` (config.js surfaced zero errors). Fixes required:
  - **components.js**: `interactiveObjects`/`cpuPins`/`ledMeshes` typed `THREE.Mesh[]`, `siliconDieMesh`/`cpuRadarRing` `THREE.Mesh | undefined`; the radar-ring material check became `mat instanceof THREE.MeshBasicMaterial` (the ring is created with MeshBasicMaterial — true at runtime, and it narrows the `Material | Material[]` union that the old `mat.opacity !== undefined` guard couldn't).
  - **board.js**: `boardGroup` typed `THREE.Group | undefined`; params typed on `createBoard`/`updateBoardParallax`/`lerpFactor`/`drawHatchedPour`/`drawCrosshair`; the mounting-holes `forEach` captures `const board = boardGroup` — a module-level `let` is 'possibly undefined' inside closures even right after assignment, so the closure needs a captured const.
  - **particles.js**: `Particle` typedef (`{mesh, points, progress, baseSpeed}`), `particles` typed `Particle[]`; deleted the unused `DEFAULT_PARTICLE_COLOR` const (TS6133).
  - **project-chips.js**: `FlickerLed` typedef (`{mat: MeshStandardMaterial, seed}`), `flickerLeds`/`steadyLeds` typed; `buildBreadboardPatch(group, goldMat)` dropped its unused `goldMat` param (TS6133) and the call site now passes `(group)` only.
  - **fallback.js**: `canvas.getContext('webgl') || getContext('experimental-webgl')` asserted to `WebGLRenderingContext | null` (the raw `RenderingContext` union has no `getExtension`); `setupCleanup` dropped its unused `camera` param (main.js now calls `setupCleanup(scene, renderer)`); `disposeScene`/`disposeMaterial` params typed, with `disposeMaterial` casting to `MeshStandardMaterial` for the defensive map/lightMap/envMap disposal.
  - **sections.js**: `esc(str)` param typed; the `querySelectorAll(...).forEach` callbacks cast each element inside the body (`/** @type {HTMLAnchorElement} */ (a)`) — an arrow-param annotation is rejected by the NodeList.forEach callback signature (contravariance).
  - **boot.js**: added `import * as THREE` for the silicon-die settle — `siliconDieMesh.material.opacity = 0.65` became `instanceof THREE.MeshBasicMaterial` narrowing (the die material is MeshBasicMaterial in components.js).
  - **hover.js**: the last `/** @type {any} */` interop cast deleted — `interactiveObjects` is typed `THREE.Mesh[]` now, so the filter callback narrows without a cast.
  - **Verified**: `npm run typecheck` exits 0 across all 12 modules; `npm run build` green; live journey re-checked (boot → overlay gone, all 6 panels live, canvas WebGL rendering); console clean.
- **2026-08 Session — Dead-export sweep + typecheck script + schema.ts removed**:
  - De-exported four symbols verified dead by full-repo search (zero importers): `vias` (traces.js — export + the `vias.push(viaGroup)` collection dropped; the via groups are still created and added to the board), `silkscreenMesh` (board.js — now module-private `let`, still assigned and added to the board), `targetMouse` (hover.js — now module-private `const`, still fed by `updateMouseCoords` and read by `checkHover`), `setCameraAtT` (journey.js — now internal, still called by the ScrollTrigger `onUpdate`). All type-only de-exports — runtime identical.
  - `package.json` gained `"typecheck": "tsc --noEmit"`; **`src/schema.ts` deleted** (its only purpose was giving tsc a `.ts` file — the real checks are the `// @ts-check` JS modules). `npm run typecheck` passes with zero `.ts` files in src. README + claude.md protocol now use `npm run typecheck`; the stale open item is marked DONE.
  - **Verified**: `npm run typecheck` + build green; grep confirms no `export` remains on the four symbols; live journey re-checked (boot → hero active, vignette 0.35). Reviewer: no defects.
- **2026-08 Session — Plan 001 executed (nav sheen transform-only) + plans 001/002 marked DONE**:
  - Per user direction, plan 001 was executed as a **translateX conversion, not deletion**: `style.css` `.nav-btn::after` sweep moved from `left: -100% → 100%` (`transition: left 0.5s` — a layout property on a hot control) to `left: 0` + `transform: translateX(-100% → 100%)` with `transition: transform var(--duration-base) var(--ease-out)` (0.3s, token-eased). Two deviations from the plan body, both required by that strategy: `overflow: hidden` stays on `.nav-btn` (the sweep still needs clipping), and `.hud-nav .nav-btn.nav-active::after` in `scroll.css` gained `transition: none` so the ENIG gold pad doesn't inherit a transform slide-in on section change (plan finding #3 stays fixed).
  - `plans/README.md`: 001 and 002 marked **DONE** with an execution log; 002 was already implemented in the hyperframes-animation session (`.header-underline` pinned `280px` + `scaleX(0→1)`, center origin — matches the plan exactly). Note added for 003 that its excerpts assume the sheen was deleted — re-read current `style.css` before executing.
  - **Verified**: build + tsc green; `grep transition: left` clean in style.css; live computed-style check — sheen transition is `transform 0.3s` at `translateX(-100%)`, and the gold pad's computed transition is `none` with the transform identical before/after a nav-active toggle (snaps instantly, no slide).
- **2026-08 Session — animejs discipline audit — last unseeded randomness removed**:
  - Audited the whole animation surface for the animejs contract (deterministic, finite, single-clock, no unseeded randomness, no animations built in timers):
    - `Math.random` / `Date.now` / `performance.now` — **one hit**, fixed: `project-chips.js` seeded each breadboard LED flicker phase with `Math.random() * 100`, so the "in build" LEDs pulsed differently on every page load and every `?og=1` capture. The flicker formula was already deterministic (`sin(elapsed·7 + seed) · sin(elapsed·13.7 + seed·2)`); now the seed is `flickerLeds.length * 2.4` — a fixed per-LED phase offset, out of lockstep, reproducible every visit.
    - `setTimeout` — only the `scene.js` resize debounce remains (a debounce, not an animation); boot is setTimeout-free.
    - `repeat: -1` / `Infinity` — zero hits; all GSAP pulses are finite (boot flashes `repeat: 1`, LED blinks `repeat: 3`, silicon die `repeat: 7`).
    - `requestAnimationFrame` — exactly two: the `scene.js` tick loop (the single clock — correct) and a one-shot `ScrollTrigger.refresh()` scheduling in journey.js.
  - **Verified**: grep clean (only the explanatory comment mentions Math.random); `npm run build` + `npx tsc --noEmit` green.
- **2026-08 Session — hyperframes-animation discipline applied (transform-only tweens + pre-calculated layout constants)**:
  - **Boot underline `width` → `scaleX`** (the skill's transform-only spatial-motion rule): `style.css` `.header-underline` now has a fixed `width: 280px` + `transform: scaleX(0); transform-origin: center;` (grows from center, the same feel as the old width draw); `boot.js` tweens `scaleX: 1` instead of `width: '280px'` in the full boot path, and the skip/lite path sets `gsap.set(underline, { scaleX: 1 })`. The old tween forced layout every frame of the 1s draw; scaleX is compositor-only. Verified live: underline draws to `matrix(1,0,0,1,0,0)` after boot. *(Note: this supersedes plan 002's left-origin suggestion for the underline — center origin was chosen deliberately to preserve the shipped centered-grow visual.)*
  - **Panel dimensions measured once, not per frame** (the skill's pre-calculated layout constants rule): `updateJourneyEffects` read `panel.offsetWidth`/`offsetHeight` every animation frame — forcing layout on every scroll-scrub frame. New `panelSizeCache` (keyed per panel) measures once per panel; `invalidatePanelSizeCache()` runs on `window.resize` (media-query widths) and in the existing `document.fonts.ready` hook (font-swap heights). Fallbacks (`Math.min(480, vw-40)` / `Math.min(300, vh*0.5)`) and the `.ds-panel-wide` 980px handling are preserved. Verified live: panel-projects still right-anchors with correct cached-width placement.
  - **Verified**: tsc + build green; live journey re-checked (boot → underline drawn via scaleX → panel anchors on scroll); console clean; reviewer-clean.
- **2026-08 Session — README refresh (uncommitted)**: rewrote `README.md` to the shipped state — scroll-journey-only interaction model table, daughterboard datasheet panels, fab-bench backdrop + shadow grounding, board-first hero, deterministic boot, deep links, single-CTA rule, `og-preview.png` card embedded, and the checkJs verification protocol. Removed stale content for deleted systems (legacy hover-boost mode, tooltips/camera-states, Orbitron/Share Tech Mono fonts, canvas-renderer fallback). Setup/stack/structure sections corrected to Three.js r185 + GSAP 3.15 + Chakra Petch/Fragment Mono/Instrument Sans.
- **2026-08 Session — OG social card shipped (`d7b8f52`)**: ran the `.freebuff/og-tools` capture pipeline (headless Chrome + SwiftShader WebGL against `?og=1` instant-boot mode) and generated `public/og-preview.png` (1200×630). Verified not black — 96.7% non-black cells, 20.8% FR-4 green, 29 color buckets, board centered with lit traces, zero console errors. Committed the PNG alone (kept the redesign batch separate), confirmed Vite copies it into `dist/` so the `og:image` meta resolves. LinkedIn/Discord cache scrapes — re-scrape via their debugger tools after deploy.
- **2026-08 Session — Big batch committed + pushed (`fa6e363`)**: committed the daughterboard redesign (scroll.css), full-board framing + vignette (journey.js), fab-bench backdrop + shadow catcher (scene.js), board-first hero, dead-symbol sweep (main.js/board.js), and checkJs on journey/boot (tsconfig.json, boot.js) as one cohesive commit; pushed `cd8b451..fa6e363` to `origin master`. Validation before push: tsc + build green.
- **2026-08 Session — traces.js typed (last `@type {any}` interop casts removed)**:
  - Added `// @ts-check` to `src/three/traces.js` with a `TraceRoute` JSDoc typedef (`component: string`, `points: THREE.Vector3[]`, `width: number`, `meshes: THREE.Mesh[]`); `traceData` typed `TraceRoute[]`, `vias` typed `THREE.Group[]`; params typed on `createTraces`/`addTraceMesh`/`addVia`.
  - **journey.js**: `pulseArrival`'s `/** @type {any} */` casts on the trace callbacks are gone — `t`/`m` now flow from the typedef, and the material check became `mat instanceof THREE.MeshStandardMaterial` (trace segments always use that material; narrows the `Material | Material[]` union). **boot.js**: the `/** @type {any} */` mesh cast dropped — `mesh.material` is directly assignable to gsap's `TweenTarget`.
  - particles.js (unchecked) now receives the typed `TraceRoute[]` too — same runtime data, but its `traceData.forEach(trace => …)` reads are type-checkable when it gets `@ts-check`.
  - **Verified**: tsc exits 0 (now 6 checked files: schema + journey, boot, hover, scene, traces); build green; live journey re-checked (boot → hero active, overlay gone, console clean).
- **2026-08 Session — checkJs extended to hover.js + scene.js (full interaction core checked)**:
  - Added `// @ts-check` to `src/utils/hover.js` and `src/three/scene.js`. `npx tsc --noEmit` now type-checks the whole interaction core: journey, boot, hover, scene (+ schema.ts).
  - **hover.js**: typed module state (`raycaster`/`activeCamera`/`currentHovered`/`hoverLight` as `| null` — the runtime guards already existed), `PCB_GLOW_MAP` as `Record<string, number>`, param types for `initHover`/`handleHoverEnter`/`resetHoverMesh`, inline param annotations for the `clamp` and `updateMouseCoords` arrows, a `Record`-indexable glow map, and `/** @type {any} */` casts for the heterogeneous `mesh.material` (Standard/Basic/Phong all share `.emissive`) and the unchecked-module `interactiveObjects` filter callback.
  - **scene.js**: module exports typed as `THREE.Scene | null` etc. (honest — null until `initScene`), `tickCallbacks` as `Array<(elapsed, delta) => void>`, `canvas.getContext('2d')` asserted non-null, `checkPerformance(deltaMs)` param, `resizeTimeout = 0` + `fpsHistory: number[]` initializers, and two new **type-only guards** in the resize debounce and `animate()` (`if (!camera || !renderer) return`) — closures can't narrow module-level lets; both were already guaranteed assigned at runtime.
  - **Verified**: tsc exits 0; build green; live journey re-checked (boot → hero active, vignette 0.35, connector present, console clean).
- **2026-08 Session — Dead-symbol sweep + checkJs enabled (tsc now meaningful)**:
  - Removed dead symbols: `boardGroupRef` (declaration + assignment) and the now-unused `boardGroup` param of `initJourney` (call site is now `initJourney(camera)` in `main.js`); the unused `composer` and `isSmallViewport` imports in `main.js`; the unused `showFallbackUI` import in `board.js`.
  - **checkJs opt-in**: `tsconfig.json` now sets `"allowJs": true` (`checkJs` stays `false` — opt-in per file). Added `// @ts-check` to `src/scroll/journey.js` and `src/ui/boot.js`, so `npx tsc --noEmit` now type-checks them instead of only the trivial `schema.ts`. Journey.js needed JSDoc for its index-signature tables (`COMPONENT_WORLD`, `FIXED_CAMERAS`, `ARRIVAL_TRACE`, `stopTs`, `PATH` → `Record`/tuple types), typed module-level bindings (curves, cameraRef, connectorLine, vignetteEl, arrivalGlide, activePanelId…), params on every exported function, a `filter(Boolean)` cast for the `sections` array, `String(intensity)`/`'0.35'` for the two `style.opacity` writes (CSSStyleDeclaration.opacity is a string property), and `lookCurve` added to the `setCameraAtT` null guard. Boot.js needed param types + `overlay` null guards. Behavior unchanged — the guards and values already existed at runtime.
  - **Verified**: `npx tsc --noEmit` exits 0; `npm run build` green; live journey re-checked (boot → hero panel → scroll switches to `panel-projects`); console clean.
- **2026-08 Session — Frame-clock hygiene in the 3D tick loop (threejs-animation applied)**:
  - `scene.js`: the tick-loop delta is now **clamped at the source** (`Math.min(timer.getDelta(), 0.05)` = `MAX_DELTA`). Before, a background-tab return or frame hitch handed consumers a multi-second delta that teleported delta-driven motion. `elapsed` stays unclamped (radar ring / LED flicker are sine oscillators — phase-skip is invisible).
  - `board.js`: `updateBoardParallax` lerp factors are now **delta-scaled** via `lerpFactor(k, delta) = 1 - Math.pow(1-k, delta*60)` — the same convention `hover.js` already uses. At 60fps the factor equals the old constant exactly (feel unchanged); at any other frame rate the per-second response is now identical instead of frame-rate-dependent. `main.js` passes `delta` through.
  - `particles.js`: dropped the now-redundant internal `Math.min(delta, 0.05)` — the source clamp owns the cap.
  - **Verified live**: skip-boot path healthy (overlay gone, nav clickable), tick loop runs clean, console only shows the known env shadow-map error. Build + tsc green; code review: no defects (applied its nits — comment accuracy + redundant clamp removal).
- **2026-08 Session — Boot→hero camera glide (hyperframes-animation applied)**:
  - Fixed the **boot→journey camera cut**: `scene.js` inits the camera at `(0, -2, 17)` and the boot animates only the board, so `initJourney`'s old `setCameraAtT(0)` SNAPPED to the hero stop `(0, -5.2, 13)` — a hard cut on every load, worst on the skip-boot path.
  - `journey.js` now calls `glideToHero()` instead: a GSAP timeline tweening `cameraRef.position` + `curLook` (both Vector3) to the hero config over `ARRIVAL_GLIDE_DURATION` (1.0s) with `power2.inOut` (repositioning ease per the camera rules), `onUpdate: cameraRef.lookAt(curLook)`, `onComplete` nulls the ref. Transform-space only (3D vectors — no layout properties).
  - **Interruptible**: `setCameraAtT` kills the glide via `killArrivalGlide()` on the first scroll scrub, so the tween and the scrub never fight; the glide endpoint equals `posCurve.getPoint(0)` (both derive from `FIXED_CAMERAS['sec-hero']`) → zero drift after settle.
  - Lite mode (skips `initJourney`) and `?og=1` (gets the glide — fine, captures wait anyway) unaffected.
  - **Verified live**: full boot → journey inits, hero panel active, overlay gone; scroll after boot kills the glide cleanly (projects panel + nav sync at 27%); console clean. Build + tsc green; code review: no defects (applied its nits — named duration const, `killArrivalGlide()` helper).
- **2026-08 Session — Signal-path scroll progress + keyboard nav (animejs discipline, GSAP-free)**:
  - `index.html`: HUD legend gained a signal-path readout — `.sig-path` track > `.sig-path-fill` + `.sig-path-pct` (percentage register).
  - `scroll.css`: `.sig-path`/`.sig-path-fill`/`.sig-path-pct` — fill uses `transform: scaleX(0)` with `transform-origin: left center` and a 0.08s linear transition (transform-only, no layout); ENIG-gold gradient + glow matches the fab-shop palette; `prefers-reduced-motion` gets `transition: none` for the fill.
  - `main.js`: `updateSigPath()`/`scheduleSigPath()` — the fill is a **pure function of scroll position** (`scrollY / (scrollHeight - innerHeight)`), deterministic, no wall-clock; passive `scroll` + `resize` listeners with rAF coalescing (one write per frame max); DOM refs cached after first lookup. `handleSectionKey()` — number keys 1–6 jump to sections via `navigateToSection`, ignoring modifier-held keys and form-field focus.
  - Applied the animejs skill's *principles* (deterministic, finite, single source of truth) without adding the dependency — the project is GSAP-based and the skill itself directs complex sequencing to GSAP.
  - **Verified live**: fill tracked a 50% scroll to `scaleX(0.5001)` / "50%" within 1%; key `3` → `#/projects`; guards confirmed (ctrl+5, letter `a`, focused input all ignored; `6` → `#/contact`); console clean. Build + tsc green; code review clean (cached the two DOM refs).
- **2026-08 Session — Deterministic boot sequence (hyperframes-animation applied)**:
  - `boot.js` rewritten as **one GSAP timeline with ALL-ABSOLUTE positions** (a `SCHEDULE` const in seconds — scanline 0.3, HUD 1.75, hero 2.35, subtitle 2.65, badges 2.8, canvas/board 3.45, traces 4.85, pins 5.05, LEDs 5.25, cores 5.55, status 5.85, overlay fade 6.25 → boot ends ~6.85s). No relative `+=` chaining.
  - **Typewriters are now timeline-driven proxy tweens** (`tl.to` on `{ n: 0 → text.length }` with `onUpdate` slicing `textContent`, `CPS = 33.3` chars/s) — deleted `typewriterEffect()` and `typeTerminalLine()` entirely. No `setTimeout` anywhere in the boot (the only remaining `setTimeout` in the repo is the debounced resize in `scene.js`).
  - **All flash tweens moved onto the timeline**: trace emissive flashes (staggered `SCHEDULE.traces + index*0.05`), CPU pin flashes (`+ idx*0.015`), LED blinks (`+ idx*0.1`) — previously created ad-hoc inside `tl.add` callbacks; status text updates via `tl.call(..., [], absPos)`.
  - **Terminal rebuild is synchronous**: the 3 `BOOT_LINES` divs are created + their typewriter tweens scheduled at boot time; the `#terminal-status-text` register is re-appended via `tl.call` after the last line (~4.2s) — all `updateTerminalText` calls (≥4.85s) happen after it's attached again.
  - **Silicon die pulse** stays a detached FINITE `gsap.to` (`repeat: 7`, `delay: SCHEDULE.cores`) so it can't stretch the timeline; settles at opacity 0.65. The `?og=1` / skip-boot instant paths are unchanged.
  - **Why**: the old boot mixed wall-clock `setTimeout` typewriters inside a frame-ticked GSAP timeline — under slow renderers the timeline and text desynced and boot completion (→ `initJourney`) became non-deterministic, the root cause of the headless OG-capture failures.
  - **Verified live**: full boot types all 3 lines + subtitle, re-appends the status register, ends with overlay `opacity 0`/`display none`, hero visible, HUD ready, underline 280px, journey inits after; `?og=1` and skip-boot paths still instant. Build + tsc green. Code review: no defects.
- **2026-08 Session — Dead layout CSS sweep (no DOM behind it)**:

  - Removed from `style.css` (~140 lines): `#main-header` + `.header-container` wrappers (hero typography kept), the whole `.svg-separator` / `.trace-active-line` / `.spark` section + `traceDraw`/`sparkMove` keyframes, `.canvas-hint`, `#main-footer` + all old footer classes (`.footer-container`, `.footer-details`, `.footer-meta`, `.footer-status`), `.social-pins`/`.social-pin`/`.pin-dot`/`.pin-lbl`/`.copyright`, the `.header-nav` wrapper in the Minimalist Navbar block, and the dead `#main-header`/`.header-nav`/`.svg-separator`/footer/social-pins rules in the 768/480 media queries + reduced-motion block. Kept everything live: `.nav-btn` (HUD), `.header-underline` (boot animates it), `.ece-label`/`h1#user-name`/`.subtitle-wrap`/`.terminal-cursor`/`.badge-row`/`.stat-badge`/`.badge-lbl` (hero panel).
- **2026-08 Session — Orphaned export pruning (post legacy-stack deletion)**:
  - **`particles.js`**: removed `setHoveredTraceSpeedBoost` (only consumer was the deleted legacy hover path) and its supporting state (`speedMultiplier`, `connectedComponent`, `material` fields on particles, `BOOST_PARTICLE_COLOR`). Particles now flow at constant `baseSpeed`.
  - **`components.js`**: removed the never-visible sub-core architecture — `insideInteractiveObjects`, `cpuInsideGroup`/`gpuInsideGroup`/`oscInsideGroup`/`antInsideGroup`/`usbInsideGroup`/`vrInsideGroup` + all their meshes (core_*, proj_core_*, edu_plate_*, ant_receiver, usb_contact_*, vr_fin_*). These groups were always `visible = false` and only toggled by the deleted `toggleComponentShells`. Kept: `siliconDieMesh`, `cpuRadarRing`, `cpuPins`, `ledMeshes`, `interactiveObjects` (all still used by boot/journey/hover). The LED domes stay (they're the visible LED array), only their `insideInteractiveObjects` registration was dropped.
  - `boot.js` retains the **`?og=1` capture mode** (added for the shelved OG-card task): same instant path as a return visit, with the hero panel set inline (CSS transitions don't tick under software rendering). Harmless; used only when the query param is present.
- **2026-08 Session — Deep links + boot skip**:
  - **Hash routing** (`main.js`): `SECTION_HASHES` maps `sec-*` → `#/about`-style slugs; `navigateToSection()` does `history.pushState` + `scrollToSection` (nav buttons + brand link); `applyHashNavigation()` listens for `hashchange` + `popstate` so the back/forward buttons and manually edited hashes navigate sections; on first load an existing hash is honored after `initJourney`. Hero = no hash (clean URL). Unknown hashes are ignored.
  - **Boot skip** (`boot.js`): `sessionStorage['psb-booted']` flag (try/catch-guarded) — return visitors in the same tab take the instant path (same as lite mode: overlay hidden, board at rest, HUD + badges + particles on). For the full-journey fast path the hero panel reveal uses `clearProps: 'opacity,visibility'` so the `.panel-active` CSS toggle keeps ownership (gotcha #1).
- **2026-08 Session — Legacy interaction stack deleted (single scroll-journey model)**:
  - **Deleted files**: `src/utils/camera-states.js` (zoom state machine), `src/ui/sidepanel.js` (drawer), `src/ui/tooltip.js`, `src/ui/pcb-hud.js` (hover telemetry HUD).
  - **`src/utils/hover.js`** rewritten as a pure hover-glow module: removed click-to-zoom handler, `viewState` branches, `toggleComponentShells`, `updateCamera`, tooltip/HUD/trace-boost calls, `triggerComponentAction` export. Kept: bounded pointer clamp, `targetMouse` inertia, parallax feed, glow/scale/hover-light. Exports are now only `mouse`, `targetMouse`, `initHover`, `checkHover`.
  - **`main.js`**: dropped `initTooltip`/`initSidePanel`/`openSidePanel`/`closeSidePanel`/`triggerComponentAction` imports + the tooltip/sidepanel init guards + the `data-ref` legacy nav fallback + `window.openSidePanel/closeSidePanel` globals.
  - **`index.html`**: removed `#pcb-tooltip`.
  - **`style.css`**: removed ~600 lines of legacy CSS (`.pcb-tooltip-hud`, `#component-panel`, section-7 side-panel builder classes, `.info-panel-layout`, `.btn-hud-control`, `.pcb-hud-layout` + all `.hud-*`, tooltip-frame classes, and their media/focus/reduced-motion references). Kept live hero classes (`.ece-label`, `h1#user-name`, `.stat-badge`, `.nav-btn`, etc.).
  - **Verified**: `npm run build` + `npx tsc --noEmit` pass; zero references to deleted modules remain; journey mode re-checked live.
- **Panel Activation Refactor**: `updateJourneyEffects` no longer scans camera distance thresholds. Panel activation is now a **pure function of the current scroll leg** — `setLegState(destination, source, progress)` runs in each ScrollTrigger `onUpdate` and switches sections at 0.55 progress (0.05 boundary band vs. 0.5). Deleted the hysteresis/cooldown machinery (`ARRIVED_THRESHOLD`, `LEFT_THRESHOLD`, `DEACTIVATE_FRAMES`, `inAnyZone` second scan). Activation is applied idempotently each frame via the `activePanelId` early-return in `setActivePanel`. Also gated panel driving behind `journeyReady` (set at the end of `initJourney`) so the boot sequence's GSAP inline styles on the hero panel are never fought mid-boot.
- **Build Fixes**: Fixed malformed font fallback link tag in `index.html` and removed duplicate snippet syntax error in `src/three/board.js`.
- **CSS Improvements**: Added design system tokens (`--space-*`, `--radius-*`, `--depth-*`) in `style.css`, enhanced focus states, and skip-to-content links for accessibility.
- **Card Redesign (daughterboard datasheets)**: Replaced the flat glass `.ds-panel` cards with physical-PCB daughterboards — FR-4 glass-weave background stack, four corner mounting holes (radial-gradient layers), a gold edge-connector pad strip via `border-image` (slice `0 0 12 0`, pinned in the border box so it never scrolls), and a seated double-shadow. Internal components upgraded: `.ds-ref` is now a via-node + fading copper trace, `.ds-title` gets a gold pad tick (contact panel overrides it), spec tables read as pin tables (gold left tick), skill pills are SMD components with silver end-caps, project cards are mini-PCBs (gold trace top border, corner screw hole, bottom pad strip, LED-glow shipped status), timeline nodes got copper-plated via centers with hover glow, and body copy switched from mono to Instrument Sans (`--font-body`) for readability. Reduced-motion block extended. Verified live across hero/about/projects/skills/experience; build + tsc green; reviewer-clean.
- **Full-board framing + fab-bench backdrop**: The hero/contact cameras clipped the board's bottom edge at FOV 45° (z=13/14 → the board rendered "only half" on short desktop viewports). Pulled both stops to z=23 (hero `(0,-5.4,23)` look `(0,0.2,0)`) so the 15-unit board fits with ~2-unit margins, and softened the arrival vignette ceiling 0.85 → 0.6. Added `createBackdropTexture()` in scene.js — a 1024² CanvasTexture (FR-4 gradient, soldermask-green + ENIG-gold + signal glows, 64px fabrication grid with plated gold vias, light baked vignette) set as `scene.background`, plus a matching CSS fallback layer behind `#canvas-container`. The view is never an empty void around the board.
- **Board grounding (shadow catcher)**: Added a transparent bench plane (`ShadowMaterial` opacity 0.38, PlaneGeometry 36×36 at y=-8.6, receiveShadow) below the board so the already-enabled shadows (board + components all cast) land on a visible band behind/below the board — it reads as seated in the fab-bench backdrop instead of hovering. Enlarged `dirLight1`'s shadow frustum (ortho ±10 + `updateProjectionMatrix`, far 25→35, bias -0.0005→-0.001) since the default ±5 box clipped the cast shadow to a hard stripe; kept 1024² map so the per-frame shadow fill matches the original budget (the FPS guardrail scales bloom only, not shadows).
- **Board-first hero composition**: Split the shared hero/contact centering rule in `scroll.css` — contact stays center-anchored (full-board closer), while `#panel-hero` is now right-anchored (`right: 24px`, `translateY(-50%)` centered) like the component sections, so the fully-framed board's chips and traces show around the datasheet at the establishing shot instead of hiding behind it. Below 900px (and on phones ≤640) the hero recenters since a 620px panel would fully cover the board there. Verified: hero right-anchored + vertical-centered, journey scroll/panel positioning unaffected, build + tsc green, reviewer-clean after closing the 641-820px dead-zone.
- **Claude Blueprint Update**: Updated `claude.md` with complete architecture specification, raycasting interaction blueprint, single LinkedIn CTA constraints, and testing protocols.
- **2026-08 Session — Pointer inertia & journey-mode hardening**:
  - `hover.js`: bounded pointer clamping (NDC ±1) + 500ms smooth mouse inertia (`targetMouse` vs `mouse` lerp `* 0.08`); raycasts now use instant `targetMouse` while parallax uses smoothed `mouse`; legacy click-zoom + camera LERP disabled in `full-journey` mode.
  - `journey.js`: rewrote screen-space panel positioning (side-anchoring with viewport clamping) and **fixed wide-panel overflow** via `panel.offsetWidth`; added `body.in-hero-section` toggle; made `buildCurves()` idempotent.
  - `boot.js`: `hud-ready` applied once inside boot step 3 (duplicate add removed from `main.js`); hero panel reveal uses `clearProps`; silicon die pulse now finite (8 pulses → settle 0.65).
  - `scroll.css`: `#hud-bar.hud-ready { pointer-events: auto !important; }` (HUD was unclickable in full-journey mode); hidden HUD LinkedIn CTA (hero/contact) also gets `visibility: hidden` (tab-order fix).
  - **Verified live in browser (full-journey)**: boot → HUD interactive → nav clicks navigate each section → panels activate anchored on-screen → hero panel hidden at all other sections → HUD CTA hidden only at hero/contact. `npm run build` + `npx tsc --noEmit` pass; console clean.
- **2026-08 Session — "Fab-shop" redesign + journey bugfix (committed as `8726fef` + `05199ad`, follow-ups done)**:
  - Applied the fab-shop design (matte soldermask palette, warm silkscreen, ENIG gold accents, signal green reserved for "live") across HTML/CSS/Three.js — see the 🚦 NEXT SESSION block at the top of this file for the full change list.
  - **Fixed a Chromium `visibility`-transition deadlock** in `scroll.css` `.ds-panel` (panels froze at opacity 0 with transitions stuck at `currentTime 0` after nav clicks): visibility now flips instantly on activate and waits out the fade on deactivate (`visibility 0s` / `visibility 0s linear 0.6s`). **Applied but not yet re-verified live.**
  - Made the single-LinkedIn-CTA rule data-driven (`body.hud-cta-hidden` when the active panel has its own CTA) — About no longer shows a duplicate.
  - **All follow-ups done**: visibility fix verified live, `.freebuff/` gitignored, committed (`8726fef`/`05199ad`) + pushed to `origin master`.
- **2026-08 Session — 3D framing + animation pass (pushed as `05199ad` + follow-ups)**:
  - **Camera framing fix** (`journey.js`): component stops were viewed horizontally at component height (`CAMERA_OFFSET (0,0,1.915)`) — chips read as edge-on slivers. Now elevated 3/4 angle: `CAMERA_OFFSET (0, 2.6, 4.2)`, `LOOK_AT_OFFSET (0, 0.15, 0)` (~31° elevation). Via points retuned to the same z depth (~4.2) for consistent travel speed instead of per-leg zoom pulsing.
  - **Panel transition timing** (`scroll.css`): container 0.6s→0.5s; `.ds-ref`/`.ds-body` 0.6s→0.45s, `.ds-title` 0.7s→0.5s; delays 0.2/0.3/0.4s→0.1/0.15/0.25s; replaced `transition: all` with enumerated opacity/transform.
  - **Verified live**: U1 projects near screen center with panel anchored + connector attached; projects panel fully on-screen; console clean.
- **2026-08 Session — Component quality + animation polish (animejs / hyperframes-animation / improve-animations applied)**:
  - **Component quality**: 4 plated-through mounting holes with gold rings (board.js, aligned with silkscreen markers); gold lead-frame outlines on U1/U2 + silver seam on Y1 (components.js); hemisphere light for material depth (scene.js); silkscreen adds: mounting-hole rings, pin-1 dot, `Pb-FREE / NO-CLEAN` + `IPC-A-610 CLASS 2` fab markings.
  - **Animation quality**: power-on pulse on arrival (journey.js `pulseArrival` — U1 radar+die flash; U2/C1/J1 trace emissive flash, reusing boot's trace-flash language); connector line dashes march toward the panel (`signal-flow` keyframe, scroll.css); all remaining `transition: all` enumerated (style.css).
  - **Verified**: build + tsc pass; console clean; tick loop live (vignette 0.4→0.85 on proximity, connector attached, pulse fired without errors).
- **2026-08 Session — Split-screen layout: fixed datasheet sidebar (frontend-design + hyperframes-animation applied)**:
  - **The split**: every section (About, Projects, Skills, Experience, Contact + hero + focused-project detail) now shares the hero's split-screen pattern — canvas owns the LEFT 58%, and ALL seven `.ds-panel`s are pinned to the SAME fixed sidebar box in the right 42% (`body.full-journey .ds-panel`: `top: 84px; bottom: 24px; left: calc(58% + 24px); right: 24px; width: auto; max-width: 760px; margin auto`). Identical daughterboard chrome + position across sections; only inner content changes, via a pure-opacity 0.3s cross-fade (no transform — the panel never moves/resizes/repositions). The old per-component JS positioning (`positionPanelAt`/`getPanelSize`/panelSizeCache) is deleted; the connector line now draws from the component to the sidebar's left edge (`drawConnector` — only the component end travels).
  - **Canvas sizing (the stretch/squash guard)**: `scene.js` exports `getCanvasViewportSize()` (measures `#canvas-container`, which is 58%×100vh on desktop / 100vw×48vh below 768px) and `syncCanvasSize()` — camera.aspect, `renderer.setSize(w, h, false)` (CSS owns layout), composer resolution, and bloom pass size all derive from it. CRITICAL FIX: `initScene` runs BEFORE main.js adds the `full-journey` class (which shrinks the container to 58%), so the initial full-viewport aspect was stale — main.js now calls `syncCanvasSize()` right after the mode class lands. Verified: camera.aspect == canvas w/h exactly (0.4747 on the test viewport), renderer buffer == 487×1026.
  - **Hero framing is aspect-aware**: the old hardcoded z=23 fit only the vertical 45° FOV; on the narrower left-58% canvas the horizontal FOV binds. `getHeroFramingZ()` computes z to fit the 15-unit board in BOTH axes (z = max(vert, horiz)) at build/glide time, applied via `getCameraConfigForStop`; curves rebuild on resize (debounced). Component stops unchanged — zoom-to-component continues exactly as before, centered in the left region (verified: About stop (0, 3.6, 4.29)).
  - **Three polish items**: (1) top-edge `#sig-meter` — a thin 3px signal-strength rail with segmented gold→signal fill (scaleX) + glowing head LED (translateX in vw, transform-only), driven by the existing `updateSigPath` scroll readout (verified tracking the HUD sig-path pct exactly). (2) About cursor-tilt — `updateBoardParallax` is now section-aware (`getActiveSectionId` exported from journey.js, passed from main.js): About gets a boosted tilt capped at ±0.03 rad (~1.7°, verified settles at 0.0299 vs 0.003 elsewhere), so "move cursor to tilt board" is felt without fighting the scroll camera. (3) easing — `--ease-in-out` redefined to `cubic-bezier(0.42,0,0.58,1)` (≈ GSAP power2.inOut): the sidebar cross-fade, inner-content staggers, and camera arrival glides now share ONE curve.
  - **Mobile (<768px)**: never attempts the split — config `isSmallViewport` already routes to lite mode; canvas becomes a 100vw×48vh top strip (`@media (max-width: 767px)`) with panels stacking in flow. Projects grid compacts to 2 columns inside the 42vw sidebar.
  - **Verified live**: all 7 panels report pixel-identical boxes (511,84,305×918); exactly one panel visible per activation toggle (cascade forced via transition:none — this webview freezes CSS transitions, the visibility-flip pattern is unchanged and documented); chip-focus detail panel swaps in the SAME sidebar box; connector x2 == 58%+24px; meter head ↔ fill in lockstep; console clean (only the pre-existing SwiftShader shaderSource artifact). typecheck + build green.
- **2026-08 Session — journey legs collapsed to ~1.2 screens (dead scroll road killed)**: with the sidebar now FIXED, the old 180–300vh per-section tracks were pure dead road — ~13 viewport-heights of scroll for 6 sections, ~7 of it empty. (a) `scroll.css`: `.journey-sec` / `.journey-sec-tall` retuned to `min-height: 120vh` (the stale per-ID 150/200/220/300vh overrides deleted — sections contain only fixed panels, so height is pure scroll-track). (b) `journey.js` leg triggers: `'top 95% → top 5%'` → `'top bottom' → 'top top'` — exactly 100vh of scroll per leg, so the camera arrives at the stop precisely when the section fills the viewport, then parks 20vh while the next enters. (c) `scrollToSection` now targets `el.offsetTop` (was `offsetTop + 0.45 * innerHeight` — a relic of the overlay era that landed ~0.45 screens past the stop). **Verified live**: sections at 0/1054/2107/3161/4214/5268 (1.2 screens apart; total 7.2 screens vs ~11.9 before), leg windows exactly [176,1054]/[1229,2107]/[2283,3161]/[3336,4214]/[4390,5268], and the camera lands EXACTLY on each stop by its leg's end — hero (0,-5.4,41.43), about (0,3.6,4.29), projects (-3.2,7.1,4.29), skills (3.2,7.1,4.29), experience (0,-4.7,4.29), contact (0,-5.2,41.43); scrollToSection('sec-about'/'sec-skills') lands on the stop pose with panel+nav activation correct. **Tester note**: preview_evaluate has a 10s cap — an async loop exceeding it becomes a ZOMBIE that keeps scrolling and corrupts later readings; kill with a long idle wait before re-testing, and re-check `scrollY` after every scroll. Hero/contact framing z (41.43 here) is aspect-correct for the live canvas (486×878, aspect 0.5535) — the 48.32 recorded in the split-layout session was a different webview size, not a regression. typecheck + build green. Uncommitted with the rest of the batch.
- **2026-08 Session — scope-probe custom cursor (pointer:fine desktop)**: new `src/ui/cursor.js` + `#cursor-probe` DOM (index.html, sibling `.probe-halo` — a transformed ancestor would hijack fixed positioning) + CSS block in style.css section 1b. The pointer is the board's test probe: a 1px graticule crosshair (4 arms, 3px gap, `--arm-scale` var per state) + signal-green tip + a floating readout chip showing the ACTIVE panel's signal (PWR · IDLE / VCC 3.3V / BUS 5V / CLK 16MHz / REV 1.0 / TX/RX) read from the DOM (`.ds-panel.panel-active` → SIGNAL_MAP — no module coupling to journey.js). Three states via body classes: default (green), **probe** (hover.js raycast hit → `body.probe-target` toggled in handleHoverEnter / checkHover exit → arms pull to 0.55, gold, readout MEASURE — the click affordance), **link** (DOM `a, button, [role=button], .skill-pill, .project-card, .nav-btn, .secondary-link` via e.target.closest → arms extend to 1.45, gold, readout fades). Trail halo lerps toward the tip (frame-rate-independent `1-pow(0.82, delta*60)`, same pattern as hover.js parallax). Transform-only writes (translate3d, will-change), `pointer-events:none`, z-20000 (above the z-10000 boot overlay). Native cursor suppressed via `body.custom-cursor, body.custom-cursor * { cursor:none !important }` (wins over hover.js's inline cursor styles). Skipped in lite mode (reduced motion / touch — native cursor stays) + `@media (pointer: coarse)` and `@media (prefers-reduced-motion: reduce)` belt-and-suspenders. **Verified live**: transform tracks (translate3d(800px,500px,0)), trail mid-lerp (221,20→585,322→800,500 over 2.5s), link/probe classes flip, probe arm+tip go ENIG gold (rgb(201,162,75)), readout MEASURE on probe, link fade rule = 0 (computed 0.95 only because this webview freezes CSS transitions), arm default transform matrix(1,0,0,1,0,-6) exact, console clean (only the pre-existing SwiftShader shaderSource artifact). typecheck + build green. Uncommitted with the rest of the batch.
- **2026-08 Session — the horn + sustained current (buzzer beep, traveling trace dot)**: (1) **BZ1 piezo buzzer** — new COMPONENT 10 in components.js at board-local (4.2, -1.9): brass disc (CylinderGeometry, dedicated piezoMat with emissive 0x3ee6a0 @0 dark until pulsed — "if it glows, it's live"), dark center dimple, two gold wire legs, additive sound-wave RingGeometry (visible:false at rest). Clicking it fires `pulseBuzzer()` (scale pulse 1.28 yoyo, emissive flash 2.4 yoyo, expanding ring 0.25→1.7 scale + opacity 0.9→0 over 0.55s power2.out, then hidden) + `beepBuzzer()` from the new `src/utils/buzzer.js` (lazy AudioContext created on the first user gesture, 2.7kHz square wave — real piezo resonant band — double 90ms beep with 60ms gap, exponential attack/decay envelope, peak 0.1; no-op on failure so the visual never silently dies). Click dispatch: hover.js `setBuzzerHandler` + BUZZER type check in the canvas click raycast (PROJECT still goes to focusProject). **Bug caught during dev**: `gsap.fromTo(ring, { scale: 0.25, opacity: 0.9 }, { scale: 1.7, ... })` — a scalar `scale` tween on a THREE Object3D throws "Cannot assign to read only property 'scale'" (scale is a Vector3); fixed by tweening ring.scale components + material.opacity separately. (2) **Traveling current dot** — traces.js caches one CatmullRomCurve3 per component (first route wins), one shared emissive dot (`name: 'trace-current-dot'`, sphere 0.07, MeshStandardMaterial emissive 0x3ee6a0 @2.5 — blooms like the electrons) added to boardGroup, driven by `updateTraceCurrent(elapsed, activeSectionId)` from the tick loop (main.js reuses the single getActiveSectionId() call alongside parallax). SECTION_TRACE maps projects→U2, skills→C1, experience→J1 (about/hero/contact have no trace → dot hidden; the arrival pulse in journey.js is the flash, this is the sustained current). t = (elapsed × 0.3) % 1 — power flows CPU→component, ~1.3–2 world u/s; z lifted +0.025 above the trace. Reduced-motion: dot pinned static at the trace source (reads powered, never flickers — same pattern as the radar ring). **Verified live**: canvas click at the disc's projected NDC fired the full chain (ring visible → scale 1.20 → hidden after 700ms, emissive peaked 2.39), beepBuzzer no-throw, cursor readout BEEP over the buzzer (hover.js sets body.dataset.hoverType='BUZZER', cursor.js probes it), dot moves along C1 ((1.00,3.80)→(1.66,3.82)) / U2 (-1.18,3.19) / J1 (0.00,-3.55), hidden on hero, console clean post-fix (zero new errors; the `scale` exceptions in the pre-clear buffer were the pre-fix calls). typecheck + build green. Uncommitted with the rest of the batch.
- **2026-08 Session — live HUD scope readout (probe over any component)**: new `#hud-scope` chip in the HUD legend (index.html, after the REV mark; IDs `hud-scope-ref` / `hud-scope-val`) showing the probe's measurement while the pointer is over any interactive component. hover.js owns it: `SCOPE_MAP` (Record<ref, {v, f, state}>) with realistic per-part values — U1 3.3V/27MHz/RUNNING, U2 1.1V/16MHz/ACCEL, C1–C4 decoupling (100nF/10µF), Y1 27.000MHz, ANT1 2.4GHz TX/RX, J1 5V/480Mbps LINK, VR1 5V→3.3V, D1-D7 2.0V/20mA LIT, RN1 4.7kΩ, TP1 5V / TP2 GND, BZ1 2.7kHz SILENT — plus a PROJECT branch that parses the status straight out of the chip's `componentName` ("TITLE — SOLDERED (SHIPPED)") so chips report live data with zero extra coupling. `setScopeReadout`/`clearScopeReadout` fill/clear the chip (lazy DOM lookup); `body.hud-scope-live` lights it signal-green (border + ref text-shadow). Idle reads SCOPE · AWAIT PROBE; `@media (max-width: 1100px)` drops the chip so the legend never crowds the CTA. Also: hover.js now sets `body.dataset.hoverRef`, and cursor.js's probe readout got smarter — BEEP over the buzzer, MEASURE over PROJECT chips (they open datasheets), else the component's own ref (U1, C1…) since those aren't clickable. **Position fix**: the buzzer (BZ1) sat at board-local (4.2,-1.9), but the board's RIGHT half is off-frame at the establishing shot (rotated framing — board center projects to x≈423 of a 486px canvas), so it projected to (582,559) — off-canvas and undiscoverable; moved to (-1,-5.5), which projects on-canvas (385,638) near the LED array/J1. **Verified live** (real raycast hovers): U1 → "U1 · 3.3V · 27MHz · RUNNING" + cursor U1; FR1/ML1 → "… · BREADBOARD (IN BUILD)", CP1/DL1/EM1/PX1/SP1/BT1 → "… · SOLDERED (SHIPPED)" + cursor MEASURE; BZ1 → "BZ1 · 3.3V · 2.7kHz · SILENT" + cursor BEEP; exit → "SCOPE · AWAIT PROBE", live class removed. Console clean (only the pre-existing SwiftShader artifact). typecheck + build green. Uncommitted with the rest of the batch.
- **2026-08 Session — flying scope probe (WASD, the board's test probe)**: new `src/three/probe.js` + HUD hint + main.js wiring. The probe stands ON the board at a board-local coordinate (Group 'scope-probe': dark handle + signal-green band + gold cone tip whose apex touches the board surface + additive measurement-point ring), so it survives every camera move — scroll stays the primary path; the probe is additive and camera-independent. **Activation**: any WASD keydown activates probe mode (arrows fly only once active — they stay free for keyboard scrolling; tested: ArrowDown with probe inactive does NOT hijack). While active: `body.probe-flying`; the mouse raycast hover is suspended (main.js gates checkHover on `isProbeModeActive()` — one probe at a time, and `clearHover()` from hover.js clears any stale mouse glow/readout on activation); the probe glows the component under its tip (same emissive 0.5 + scale 1.04 language as the mouse hover), drives the HUD scope readout via the shared `setScopeReadout` (FR1 → "FR1 · 3.3V · — · BREADBOARD (IN BUILD)", BZ1 → "BZ1 · 3.3V · 2.7kHz · SILENT"). **Enter MEASUREs**: PROJECT → `focusProject` (verified: camera glides to exactly FR1+CHIP_FOCUS_OFFSET (-4.9,4.4,2.9), detail panel active + filled), BUZZER → `pulseBuzzer` (ring spawns). **Esc exits** (probe hides, highlight unglowed, mouse hover restored). Movement: `PROBE_SPEED 2.6` u/s, diagonals normalized, clamped to ±7.0. **Key fix**: the tip raycast was authored in board-local space but Raycaster works in world space — the board group is rotated, so a raw local ray missed everything; now the origin is `boardRef.localToWorld()` and the direction `transformDirection(boardRef.matrixWorld)`, netting the local ray (detects FR1 at (-5.09,2.81) etc.). **HUD hint** (`#hud-probe-hint` in the legend): "WASD · FLY PROBE — ENTER · MEASURE", fades in after boot (hud-ready), gentle pulse (gated under prefers-reduced-motion), fades out forever on first probe use (body.probe-used). **Specificity bug caught**: `body.probe-used .hud-probe-hint { opacity: 0 }` lost to `#hud-bar.hud-ready .hud-probe-hint` (id > 2 classes) AND the pulse animation would hold opacity anyway — fixed with `body.probe-used #hud-bar.hud-ready .hud-probe-hint { opacity: 0; animation: none }`. Also refactored hover.js's exit cleanup into the exported `clearHover()`. **Noted**: a boot-time `ScrollTrigger.refresh()` (fonts.ready → refresh) can fire leg onUpdates once, which calls setCameraAtT(0) → clears chip focus — seen once in testing when fonts finished loading mid-focus; one-time boot race, before real users focus anything. typecheck + build green; console clean. Uncommitted with the rest of the batch.
- **2026-08 Session — ride-the-trace navigation (the camera travels the actual copper)**: component→component scroll legs no longer glide straight — they ride the traceData polylines. `journey.js`: per-leg `ridePosCurves`/`rideLookCurves` built in `buildCurves` (reset there for HMR/resize). Resolver: `SECTION_COMPONENT` (about→U1, projects→U2, skills→C1, experience→J1); every traceData route is U1→X (the board is a star around the CPU), so a non-U1 pair rides **back to U1 then out** — e.g. projects→skills = reverse(U1→U2) + U1→C1, skills→experience = reverse(U1→C1) + U1→J1. Ride pose = source stop → copper points (elevated to the stops' cruise z via `srcCfg.pos.z`) → dest stop, so the leg-end invariant holds (camera lands exactly on the stop pose); look follows the copper at board level (`COMPONENT_WORLD.z + LOOK_AT_OFFSET.z`), snapping to the stops' look targets. `rideCamera(i, eased)` samples the leg's ride curve + pushes the **current-flow glow** ahead (`RIDE_GLOW_LEAD 0.06` in leg t): a signal-green emissive sphere (name 'ride-glow', MeshStandardMaterial emissive 2.5 — blooms like the electrons) created in initJourney from `cameraRef.parent` (initScene does scene.add(camera), so parent IS the scene), visible only mid-leg (p ∈ (0.001, 0.999)), hidden while a chip is focused (updateJourneyEffects). Leg onUpdate: `if (!rideCamera(i, eased)) setCameraAtT(...)` — hero/contact legs (no component) return false and keep the straight glide. **Verified live** (viewport had grown to 1026px, sections re-spaced to 1231px — leg windows now [205,1231]/[1436,2462]/[2668,3694]/[3899,4925]/[5130,6156]; the webview's sub-1FPS scrub crawls, so long settles needed): leg 2 mid at (-3.28,5.37,4.29) on the U1→U2 copper with glow ahead (-3.24,6.26) ✓; leg 2 end EXACTLY (-3.20,7.10,4.29) = projects stop ✓; leg 3 mid (2.27,3.96) leaving the C1 trace for the skills stop (via-U1 route) ✓; leg 4 mid exactly ON the J1 trace endpoint (0.00,-6.90) with glow ahead (0,-6.31) ✓; contact glide lands exactly (0.00,-5.20,48.32) (aspect-correct framing z for the 1026px viewport) with glow hidden ✓; panel activation intact (panel-contact at the end). typecheck + build green, console clean. Uncommitted with the rest of the batch.
- **2026-08 Session — ride-the-trace camera (the copper is the road)**: journey.js now builds CatmullRom camera paths from `traceData` polylines for the four component legs (about→projects rides C1's copper; projects→skills hops via U1; skills→experience rides J1; hero/contact keep the straight CAMERA_OFFSET glide). A current-flow glow (additive emissive sphere, `curGlow`) runs ahead of the camera along the curve, hidden on hero/contact legs. Resolver: `resolveRouteForLeg(fromIdx, toIdx)` builds a combined path from the source polyline + via-hop to the next component. Verified live at real leg midpoints: leg 2 camera `(-3.28, 5.37, 4.29)` with glow ahead at `(-3.24, 6.26)`, leg 3 via-U1 route, leg 4 lands on J1's endpoint `(0, -6.9)`, contact glide hidden-glow, all stops land exact. **Gotcha for future sessions**: the webview's GSAP scrub crawls (sub-1 FPS) — a 0.6s scrub takes many wall-seconds; always long-settle before reading poses, and leg windows are re-derived from `#journey-path` children (viewport height dependent — 1026px here → 1231px legs), not hardcoded.
- **2026-08 Session — sidebar card redesign (frontend-design pass)**: project cards restyled from decorated mini-PCBs (corner mounting hole, gold trace header, edge-pad strip, dashed building borders) to BOM line items: gold silk-stencil ref block (filled gold/dark text for shipped, gold-outline for in-build), pure-LED status (legend decodes colors; aria-label keeps it accessible), two fabrication rows (PROBLEM/STATE with silk-stenciled 42px labels), test-point link with square pad marker, no card lift (glow + border only). Card height 296→258px. Skill pills tightened (11.5px/4px 12px padding), panel scrollbar styled (scrollbar-width: thin). sections.js markup: ref block + LED with role="img" aria-label, dropped the text status badges. Reduced-motion block already covers .proj-ds transitions + flicker. Verified live via DOM reads (shipped ref = rgba(201,162,75,0.9) bg, LED = rgb(62,230,160), building ref = gold outline; one screenshot captured pre-ref-block on the earlier card layout; webview compositor wedged after — known heavy-WebGL preview limitation). typecheck + build green. Uncommitted.
- **2026-08 Session — panel activation content cascade (hyperframes-animation)**: `setActivePanel` in journey.js now staggers the active panel's inner blocks (ds-ref → title stack → body rows/CTA) with GSAP `fromTo` autoAlpha 0→1 + y 12→0, 0.5s power2.out, 0.06s stagger — one orchestrated arrival beat over the flat 0.3s CSS cross-fade (which still owns the panel chrome). Excludes the hidden .headline-twin (title sweep owns it), guards on `body.full-journey` (lite mode stays static), `clearProps: 'transform'` so no residue. Verified live mid-flight (opacities 1/0.81/0.67/1/0.19 across blocks) and settled (all 1, transforms cleared) on about + skills. Note: hero panel also cascades after boot (initJourney's setActivePanel) — intended. Reduced-motion: CSS transitions/flicker already gated; GSAP stagger is short opacity+y, consistent with existing reduced-motion posture. typecheck + build green. Uncommitted.
- **2026-08 Session — shipped-LED breathe (threejs-animation)**: `updateProjectChips` in src/three/project-chips.js now breathes the steady (shipped-chip) LED materials around their 1.4 build-time base — `1.4 + Math.sin(elapsed * 0.9 + seed) * 0.08`, ~7s period, seeded per LED at build time from the chip's board position (same deterministic seeded-phase pattern as the breadboard flicker) so the array breathes slightly out of sync. Gated by `STEADY_REDUCED_MOTION` (matchMedia prefers-reduced-motion, same gate as the radar ring + current dot). **Critical catch during dev**: the LEDs are created at emissiveIntensity 1.4, so the breathe must orbit the base (1.4 ± 0.08) — an initial 0.5 base would slam every shipped LED from 1.4 → 0.5 on the first tick (visible "LED dimmed" glitch). Focus flash (journey.js gsap tween, overwrite) still dominates during its short run. Verification: typecheck + build green; the preview webview's rAF loop is frozen (radar ring static) and the dev module double-instance hides the live materials, so live value-checking was blocked — the pattern is identical to the proven flicker path. Uncommitted.
- **2026-08 Session — coordinated resize (three discipline)**: three independent debounced resize listeners (scene `syncCanvasSize` 100ms, journey curve rebuild 200ms, sig-path immediate) raced with undefined ordering — a stale read could leave the hero/contact framing z off until the next resize, and journey's resize never called `ScrollTrigger.refresh()` (leg windows went stale on resize). Fix: journey.js exports `resizeJourney()` (journeyReady-gated: `buildCurves()` + rAF `ScrollTrigger.refresh()`), and main.js owns ONE 120ms-debounced coordinator in defined order: `syncCanvasSize()` → `resizeJourney()` → `scheduleSigPath()`. Journey's own 200ms listener now calls `resizeJourney()` too — kept as a resilience net for HMR/re-entry (idempotent), not the primary path. typecheck + build green. Verification constrained by the frozen webview (rAF dead, module double-instance) — the coordinator path is straightforward and the module export is confirmed present. Uncommitted.
- **2026-08 Session — hyperframes-animation rule audit (2nd pass):** Connector dashes were the one infinite animation missing a reduced-motion gate — `signal-flow 0.9s linear infinite` on `.connector-path` kept marching for `prefers-reduced-motion: reduce` users while every other infinite animation (scroll-hint, building LED flicker, probe hint pulse) was frozen. Added `.connector-path { animation: none !important; }` to the reduced-motion block (freezes dashes, line stays dashed). Also executed plan 006: `.nav-ref` now has `transition: opacity var(--duration-fast) var(--ease-out)` so the ref glyph (U1/U2/…) no longer snaps 0.8→1 on section change — the nav reads as one beat with the panel cascade. Verified in served CSS + computed styles (nav-ref transition = `opacity 0.18s cubic-bezier(0.22,1,0.36,1)`), connector still marches in normal mode; typecheck + build green.
- **2026-08 Session — night bench (the power moment)**: PWR LED in the HUD legend became a real button (`id="pwr-led"`, `aria-label`, 20px hit target via background-clip:content-box) — click it or press `P` to cut the bench lights. New `src/three/power.js`: a gsap timeline fades all five bench lights (ambient/hemi/key/fill/backlight) to 4% of base on the house curve, swaps `scene.background` to a pre-painted lights-out backdrop variant (`createBackdropTexture(dark)` + `setBenchBackdrop`), boosts UnrealBloomPass strength ×2.2 (`setBloomBoost`, applied on top of the quality budget so perf downgrades still scale), beeps via the existing WebAudio horn, and fires a signal-green PointLight surge that floods the U1→U2 main bus (`CatmullRom` from `traceData`, sin(π·t) intensity envelope, parented to boardGroup so it inherits the 0.85 scale + parallax bob). Emissive traces/LEDs/die/current dot become the only light source. Interruptible (kill + rebuild on rapid toggles), reversible, surge gated by `prefers-reduced-motion`. `benchLights` registry reset each initScene for HMR idempotency. Legend a11y: removed `aria-hidden` from `.hud-legend` (it now holds a control), added it to the decorative spans. Verified: typecheck + build green, boot clean, click toggles `body.night-bench` synchronously and reversibly, rAF alive so the fades ran; screenshots still wedged in the preview webview. Uncommitted.
- **2026-08 Session — the board comes alive (copper ripple + bench sweep + probe-energized traces)**: the board stopped reading as a solid stamped part. (1) `traces.js` — `updateTraceRipple(elapsed)` floods EVERY trace with a traveling power blob from the CPU outward (sharpened sine on cumulative route distance, base 0.4 + up to 0.75, ~0.45 waves/s, ~3.2u wavelength), per-segment cloned materials so writes never fight; reduced-motion holds the static base. (2) `hover.js` → `traces.js.highlightTrace(ref, on)` — hovering a component energizes its feeding copper with a steady 1.5 + fast 0.3·sin(4t) shimmer (probe is touching the copper); 'U1' (the power source) lights every route; release decays to base via gsap with the isTweening guard so the ripple never fights the tween (same pattern as the LED breathe). (3) `board.js` — `updateBenchSweep(elapsed)`: a signal-green additive scan line + lagging trail sweep the board surface (sin-edge envelope, 6s crossing, z just above silkscreen/traces, passes UNDER components); hidden entirely under reduced motion (a static line would read as a glitch). All deterministic (elapsed-driven), no wall-clock, no per-frame allocation (scratch-free — sin math only). Verified live: typecheck + build green; console clean over boot; rAF ~90fps so ripple+sweep run every frame; synthetic mousemove grid landed a real hover on Y1 → scope readout live + highlightTrace executed on and off cleanly. Uncommitted.
- **2026-08 Session — levitation + ambient dust (the board floats)**: the board now hovers instead of sitting dead-still. (1) `board.js` — the journey branch of `updateBoardParallax` gained levitation, gated on a new `journeyLive` param + `BOARD_REDUCED_MOTION`: `position.y = sin(elapsed*0.55)*0.16` (float), `position.z = cos(elapsed*0.37)*0.07` (depth breathe), `rotation.z = sin(elapsed*0.31)*0.012` (gentle roll) — slow 8–20s periods so it reads as hover, never vibration; composes with the cursor tilt (different axes). The gate matters: boot's arrival tween owns `boardGroup.position` until ~4.65s and `hud-ready` lands at 1.75s, so the float keys off `journeyLive` (set in main.js's boot onComplete AFTER initJourney) — an early write would yank the board mid-float-up. Everything parented to the group (traces, sweep, surge light) rides along as one living object. (2) `particles.js` — `createAmbientDust` (32 additive green motes, shared geo/mat, seeded Lissajous base positions via a deterministic dustHash — no Math.random) + `updateAmbientDust(elapsed)` (slow per-axis sine drift, zero allocation); hidden entirely under reduced motion (same posture as the sweep). (3) `main.js` — `journeyLive` flag + `updateAmbientDust` wired into the tick. Verified: typecheck + build green; drove the app's real tick callbacks manually via same-URL module import (the double-instance workaround) — `boardGroup.position.y` wrote exactly `sin(10*0.55)*0.16 = -0.1129` (float path live, journeyLive true) and 32 motes moved between t=5 and t=9 (dust path live). Direct DOM observation was blocked: the webview's app render loop dies on the SwiftShader shadow-pass shaderSource exception (documented artifact), freezing the connector/vignette; rAF itself runs. Uncommitted.
- **2026-08 Session — raycast space fix (threejs-interaction)**: the hover + click raycasts converted pointer→NDC with WINDOW-relative coordinates while the camera frustum is CANVAS-sized (left-58% split on desktop, 48vh strip on mobile) — so every hover/click ray was misaimed ~21%-of-window horizontally on desktop (the cursor had to sit near the sidebar edge to hover the board's center; click-to-component could hit the wrong chip or nothing). Verified with ray-plane math: cursor over U1's true pixel aimed the ray 0.52 world units left of it (~95px at 846px viewport, proportionally constant). Fix in `src/utils/hover.js`: `updateMouseCoords` + the canvas click handler now compute NDC from `hoverCanvas.getBoundingClientRect()` (canvas-relative, clamped to the frustum edge outside it); `hoverCanvas` cached at init, `0`-size rect guarded. The smoothed `mouse` (parallax tilt) follows — now zeroed at the canvas center, the board's visual center (subtle, arguably more correct). Also executed animation-audit finding 10: `src/three/probe.js` throttles `updateTarget` to every 2nd frame during flight (frame counter, first frame still runs) — 30Hz raycast is imperceptible at 2.6 u/s. Verified: typecheck + build green; live page loaded with the fix — cursor at U1's true pixel converged `mouse` to canvas NDC (−0.0017, 0.0276) vs U1's true (0, 0.0398) and the raycast hit U1 (pre-fix it would have been (−0.424, −0.143)). Uncommitted.
- **2026-08 Session — executed plans 011 + 012 (float wake-in + focus touchdown)**: one pass over `board.js:458-461`. Wake-in: `FLOAT_WAKE_SECONDS = 2` + `floatWakeStart` snap on the first journeyLive tick, amplitudes × smoothstep(`wake`) — the boot arrival settles at y:0 and the hover emerges from stillness (the old first write was an un-ramped sine of elapsed≈6.85, up to ±0.16 ≈ 8–12px jump at the overlay fade). Focus damp: `journey.js` exports `isFocusMode()` (reads focusedChip), `main.js` threads it into `updateBoardParallax` after journeyLive, `board.js` lerps `focusDamp` toward 0.2/1 with the existing `lerpFactor(0.06, delta)` and multiplies all three float amplitudes — the focused composition steadies under the fixed camera stop (probe touchdown), resumes on release. Verified live by driving the app's real tick callbacks: first tick at elapsed 6.85 → position.y = 0 (no pop); at 2s → exact full sin(8.85·0.55)·0.16 = −0.1581; focusProject('FR1') → amplitude exactly 0.2× the live sine (0.0314 vs expected 0.0314); exitFocusMode → back to ~1× (1.01, lerp tail). typecheck + build green; plans 011/012 marked DONE in plans/README.md. Uncommitted.
- **2026-08 Session — executed plan 013 (one reduced-motion policy source)**: new `src/utils/motion-prefs.js` — a single `motionPrefs.reduced` flag with a live `matchMedia('(prefers-reduced-motion: reduce)')` listener (modern `addEventListener` + legacy `addListener` fallback). All seven per-module consts deleted (traces.js, components.js, board.js ×2 sweep+float, particles.js, power.js, project-chips.js) and every call site now reads `motionPrefs.reduced` at call time — policies moved verbatim (hide = sweep/dust, stay-but-still = radar/current dot, gate = surge/breathe, planted = float, hold-base = ripple). Kills the fragmentation (seven copies, one forgot the gate once — the connector dashes), the load-time freeze (OS changes mid-session now apply live), and the drift class. Verified: `grep REDUCED_MOTION src/` → none; typecheck + build green; live — drove the real app tick through the new source: sweep lead/trail visible with the exact envelope (0.14/0.05), 32 dust motes visible, float wrote sin(21·0.55)·0.16 = −0.136, `motionPrefs.reduced === matchMedia.matches`. Plan 013 marked DONE in plans/README.md. Uncommitted.
- **2026-08 Session — tick smoke test (tests/smoke-tick.mjs) + the bug it caught**: a headless deterministic smoke test that builds the real board modules (createBoard/createComponents/createTraces/createParticles/createProjectChips) into a bare THREE.Scene behind a minimal DOM shim (no jsdom, no deps — a null 2d-context canvas stub falls back to blank textures), registers the SAME tick pipeline main.js uses (same order, same module instances), and drives 12000 frames (~200s) asserting invariants: float within ±0.16/±0.07/±0.012, ripple within [0.4, 1.15], sweep on-board + opacity ceilings (0.15 lead / 0.06 trail), dust inside its drift box, zero NaN across 447 meshes / 170 materials, wake-in first tick y=0 (settle-pop guard), and motionPrefs.reduced forces everything static (float planted, all 170 materials frozen after the one-time LED settle, sweep+dust hidden). **It caught a real bug**: the breadboard LED flicker (project-chips.js updateProjectChips) was the one per-frame emissiveIntensity writer with NO reduced-motion gate — a strobe under prefers-reduced-motion, exactly the plan-013 policy violation. Fixed: the function now early-returns under reduced motion, snapping flicker LEDs to 0.7 and steady LEDs to 1.4 (calm powered values; the test snapshots AFTER the one-time settle so "static" means no motion). Also: src/config.js env reads now guard `import.meta.env &&` so the module graph loads in bare Node (project-chips → data/portfolio.js → config.js); the destructured boardGroup snapshot trap (namespace getters are live — read `board.boardGroup` after createBoard). Added `npm run smoke` + README verification mention. typecheck + build green. Uncommitted.
- **2026-08 Session — re-tuned the About tilt for canvas-relative mouse (raycast-fix follow-up)**: the raycast fix moved the parallax mouse's zero point to the canvas center (NDC in [-1,1] over the left 58%), which made the About boost's old `tiltScale 0.05` saturate at |mouse| = 0.6 — the outer 40% of the canvas was a dead zone with the board pinned at max tilt. Retuned `board.js:458-468`: `tiltScale` 0.05 → 0.034, so the same ±1.7° ceiling (maxTilt 0.03) now saturates at 0.03/0.034 ≈ 0.88 — the full left region responds, and the clamp still caps the overshoot when the cursor leaves the canvas edge (mouse NDC can exceed ±1). Non-About sections keep the gentle `tiltScale 0.003 / maxTilt 0.004`. Extended `tests/smoke-tick.mjs`: phase A now sweeps the cursor through a deterministic unit-circle orbit (`mouse.set(sin(i·0.013), cos(i·0.011))`) so the tilt is exercised across the whole input range including beyond-clamp, and `audit()` asserts |rotation.x| and |rotation.y| ≤ maxTilt (0.03) at every frame. Verified: smoke PASS (447 meshes / 170 materials, 12000 frames, zero violations, clamp honored at |mouse| = 1), typecheck + build green. Uncommitted.
- **2026-08 Session — gated hover raycast behind (pointer: fine)**: hover.js now holds a live `finePointer` flag (`window.matchMedia('(pointer: fine)')`, listener pattern copied from motionPrefs — addEventListener with addListener fallback, no reload needed when a device gains/loses a fine pointer). In `checkHover` the gate sits after the parallax lerp but before frameCounter/throttle/raycast — so touch users keep the board tilt (touch already fed targetMouse for parallax) but never burn the every-3rd-frame raycast and never leave a glow stuck at the last tap point. The canvas `click` handler stays ungated — taps still MEASURE/focus chips and buzz the buzzer. Verified: typecheck + build green, smoke test still PASS (hover.js is in its module graph), served module carries the gate, and live on the fine-pointer webview a real mousemove over the canvas still produced `probe-target` + live scope readout `U1` (full raycast path intact). Known edge, left as-is: on hybrids (touchscreen laptop) pointer:fine is true so a tap can still leave a glow until the next mousemove — the pointer:fine heuristic is the standard fix for the touch-only case; a touchend-clear + synthetic-mousemove suppression would be the hybrid hardening. Uncommitted.
- **2026-08 Session — executed plan 005 (cascade must not block the CTA)**: the panel-content cascade in `setActivePanel` (`src/scroll/journey.js:538-556`) now excludes `.cta-linkedin` from its `blocks` filter (alongside the existing `headline-twin` exclusion) and tightened per plan: `y: 12→10`, `duration: 0.5→0.3`, `stagger each: 0.06→0.04`; `power2.out`/`autoAlpha`/`clearProps`/`full-journey` guards untouched. About/contact CTAs + `#pdetail-link` are direct panel children → appear instantly with the panel instead of last (~0.8s wait). **Hero nuance**: the hero CTA sits inside `.hero-cta-row` (direct child), so that row still cascades as a block — the `<a>` itself was never a direct target; total hero cascade now ~0.45s vs ~0.75s. Verified: typecheck + build + smoke green; served journey.js carries the fix (`hasFix: true` — the exclusion + `duration: 0.3`); live DOM: hero CTA zero inline GSAP styles (opacity 1) at every sample; console clean across reloads. Caveat: full feel-check on about/contact needs a booted journey — the preview webview's gsap ticker was wedged on every fresh load this session (boot overlay never faded past opacity 1, hud-ready never landed), so the plan's in-browser check is left for a real browser. Plan 005 marked DONE in plans/README.md + plan file. Uncommitted.
- **2026-08 Session — executed plan 007 (keyboard-nav affordance)**: `<span class="hud-keyhint" aria-hidden="true">KEYS&nbsp;1–6&nbsp;·&nbsp;JUMP</span>` added to `.hud-legend` in index.html (after `#hud-probe-hint`, line ~169). scroll.css: base `.hud-keyhint` rule `font-size: 10px; letter-spacing: 1.5px; color: rgba(157,180,163,0.55)` — static (no pulse), rides the `#hud-bar.hud-ready` fade (bar starts opacity:0, no per-hint state needed, unlike the probe hint which needs its own because of the pulse + probe-used hide); `.hud-keyhint` added to the `@media (max-width: 900px)` hide alongside `.hud-probe-hint`; `body.lite-mode .hud-keyhint { display: none }` (the 1–6 key nav is a full-journey feature). `handleSectionKey` untouched. Verified live in the preview: element present inside `#hud-bar` with aria-hidden; computed styles exact (10px / 1.5px / rgba(157,180,163,0.55)); all three rules present in the served CSS; toggling `body.lite-mode` → display:none; the `display:none` seen at probe time is the <900px hide correctly engaging (webview innerWidth is 844px — the base rule has no display property, so the hint shows above 900px). typecheck + build + smoke green. Plan 007 marked DONE in plan file + plans/README.md — all session-2 plans (005/006/007) now DONE. Uncommitted.
- **2026-08 Session — hybrid touchscreen hardening (stuck glows on pointer:fine laptops)**: `hover.js` now carries a `HYBRID_TOUCH_WINDOW_MS = 600` suppression window (`suppressHoverUntil`). A tap on a touchscreen laptop (pointer:fine true) previously engaged the raycast glow at touchstart, then touchend left it stuck, and the browser's synthetic ~300ms-later mousemove re-lit it. The window gates BOTH the mousemove feed (synthetic tap events skipped) AND the per-frame raycast — the latter was the subtle part: even with the feed suppressed, the next checkHover tick raycasts at the STALE aim point (targetMouse still at the tap) and re-lights the glow, which my first live test caught (baseline U1 → touchend cleared → synthetic mousemove still re-lit U1). Fix: touchend/touchcancel clear the hover + open the window; checkHover returns before the raycast while it's open; mousemove is ignored inside it. Verified live with the app's real stamped modules: baseline hover U1 → touchend cleared ('') → synthetic mousemove + raycast pump stayed cleared ('') → after 700ms real mouse re-engaged U1 — pass:true. Click handler untouched (taps still MEASURE/focus). Wall-clock justified in-comment (input debouncing, never scene state). typecheck + build + smoke green; console clean. Uncommitted.
- **2026-08 Session — pointer:fine gate on the probe tip raycast**: `hover.js` now exports its live `finePointer` flag (`export let finePointer`) — the single source the mouse-hover raycast already used — and `probe.js`'s `updateTarget` early-returns on `!finePointer`. The probe is a WASD/Enter keyboard interaction; on coarse-pointer devices the per-frame tip raycast is pure cost with no user to serve (the probe can still be flown with a hardware keyboard, it just never raycasts; Enter no-ops while probeTarget stays null). Verified both paths: **coarse** — Node shim (matchMedia matches:false) drives 120 frames of probe flight via the real module and confirms zero tip raycasts (spied setScopeReadout never called); **fine** — live webview with the app's real stamped instances (finePointer true, tickCount 1): probe at rest highlights ML1 (`3.3V · — · BREADBOARD (IN BUILD)`), flight lands on U1, full scope readout live — gate doesn't break desktop. The initial empty readouts were the webview's stale board matrices (dead render loop) — refreshing `scene.updateMatrixWorld(true)` before the pump fixed the probe, same artifact as the hover session. typecheck + build + smoke green; console clean. Uncommitted.
- **2026-08 Session — raycast layer in the headless smoke test (phase C)**: `tests/smoke-tick.mjs` gained a raycast phase between the motion phases. The shim now has a window event registry (initHover's mousemove listener actually runs; dispatchEvent fires it), a query-aware matchMedia (only '(pointer: fine)' matches — reduced-motion still false for phase B), and a FAKE_CANVAS whose rect models the desktop 58% split (742.4 of 1280, left-anchored). Phase C builds a real PerspectiveCamera (fov 50, frustum matching the fake canvas), calls the app's own initHover/checkHover, sweeps six poses around the board, and for every aimable component projects its center to a canvas pixel, dispatches a real mousemove, and asserts the reported hover === an INDEPENDENT Raycaster at the same NDC (both rays see the same world, so occlusion resolves identically). This asserts the canvas-rect pointer→NDC conversion AND the raycast pipeline together — a window-relative conversion (the pre-fix bug) would aim ~21%-of-window left and fail. Result: 115 aimable component-poses across 23 unique components, 115/115 matches. Two harness bugs caught and fixed along the way: (1) the hover path energizes the feeding copper via highlightTrace — my killTweensOf froze the release tween mid-flight at 1.5, tripping phase B's ripple bound; fixed by clearHover() + explicit rippleMats reset to RIPPLE_BASE; (2) the audit's generic failure message surfaced only after I made it print the violations. README's smoke description updated to cover the raycast layer. typecheck + build + smoke green. Uncommitted.
- **2026-08 Session — live-effects pass: hover shadow + floating gold flecks**: two additive ambient effects, both deterministic and reduced-motion gated. **(1) Hover shadow** (`board.js`, scene-level — deliberately NOT parented to boardGroup because a shadow doesn't roll with the object): a baked radial-gradient blob (256px canvas texture, black → transparent) lying flat on the bench at y=-8.55 (just above the ShadowMaterial bench plane at -8.6). `updateHoverShadow()` reads the LIVE board pose every frame — `opacity = 0.34 - h*0.22` where h = (y + 0.16)/0.32 (highest board = lightest shadow), scale breathes ±6% with the depth motion. Reading the real position (not the raw sine) means the wake-in ramp and focus damp compose through the actual pose automatically. Reduced motion: fixed mid opacity (0.23) + scale 1 — stay-but-still (a planted board still grounds itself). This is the audit's "cast shadow never reacts" missed opportunity — the single biggest "it's actually floating" amplifier. **(2) Gold flecks** (`particles.js`): 12 ENIG-gold specks (`0xc9a24b`) drifting ABOVE the board, slower frequencies than the dust (0.13/0.09/0.07 vs 0.21/0.14/0.11) so the air reads layered; normal blending (not additive) so they read as material and never collide with the smoke test's additive classifiers. Hidden under reduced motion (dust posture). Verified: typecheck + build + smoke green — smoke now asserts shadow opacity ∈ [0.12, 0.34] + scale ∈ [1±0.06], 12 flecks in their box, all hidden/static under reduced (459 meshes now). Live in the preview via the app's real stamped modules: shadow found in the scene, 12 flecks, and the correlation is exact — board y 0 → opacity 0.22; after 2s sim y 0.1419 (rising) → opacity 0.1324, matching the formula (shadowTracksHeight true), flecks moving. Uncommitted.
- **2026-08 Session — alive-at-rest pass (idle drift, LED pulse, ambient signal pulses, typewriter, sound gate)**: five additive layers, none touching the scroll-to-zoom/camera-tween path. **(1) Idle camera micro-drift** (`src/three/idle.js`, new): after 3s without scroll/wheel/pointer/touch/key, the camera eases into a few-pixel sinusoidal drift (amplitude scales with camera z — 0.0022/unit → ~2-3px at EVERY framing, not a 10px slide at close stops; periods 11s/8.5s). Delta-applied so it's the last writer while idle and a scrub simply overwrites the base; eased out on interaction; skipped under reduced motion, in lite mode, and in focus mode (the focused composition is deliberately steadied). Wall-clock is documented as input gating (hybrid-touch precedent), all motion values deterministic. **(2) D1-D7 LED array** (`components.js updateLedArray`): the seven status LEDs (flat 0.1) now breathe on staggered seeded intervals (0.42-0.92 Hz, fixed hash per LED), sharpened sine peaks at 0.7; reduced motion holds calm 0.1. **(3) Ambient signal pulses** (`traces.js updateAmbientPulses`): one gold current dot (0xc8960c, emissive 1.4, slower than the green active-section dot so the layers never lockstep) continuously travels every main trace route via the cached CatmullRom curves, staggered seeded phase, independent of scroll — the "powered on, not just lit" layer. Hidden under reduced motion (dust posture). **(4) Typewriter body reveal** (`journey.js`): `.ds-body` narrative copy (About, Contact, project-detail) splits into per-char spans on panel activation and reveals at 16ms/char (TYPE_CHAR_MS, the 15-20ms brief) instead of arriving only with the cascade; reset collapses spans losslessly (textContent), so re-activation and fillProjectDetailPanel both stay safe; detail-panel chip switches type explicitly (setActivePanel early-returns on same id); skipped under reduced motion; `.ds-body.typer-active { white-space: pre-wrap }` preserves exact text. Data panels (projects grid, skills, timeline) deliberately NOT typewritten — data, not narrative. **(5) Master sound gate** (`src/utils/sound.js`, new): SND toggle in the HUD legend, muted by default; the toggle click IS the gesture that builds the AudioContext; hover/click blips (rate-limited) + the buzzer horn all behind the one flag (buzzer.js now early-returns on !isSoundEnabled — visual pulse still plays). Hover component feedback already existed (scale + emissive + light + trace energize); bumped 1.04→1.05 per the brief (hover.js + probe.js). **WASD probe decision**: kept — it's already fully opt-in (WASD keypress activates; arrows only fly once active; Esc exits; tip raycast pointer:fine-gated), so it can't interfere with the idle layers. Verified: typecheck + build + smoke green — smoke now asserts LED ∈ [0.1,0.7], 10 pulses on-board + hidden under reduced, idle-drift offset bounds/determinism + interaction resets the clock (469 meshes, 181 materials, phases A-D). Live in the preview via the app's real stamped modules: SND toggle flips label/aria-pressed/body.sound-on both ways; typewriter full cycle on the live journey (scrollToSection → 577 char spans reveal progressively — char 120 visible while char 577 mid-fade — then collapse to plain text on leave); driving the app's own tick closures showed the LED breathe (0.373→0.611), a pulse traveling (-0.57→-2.44), and the idle drift moving the camera ~0.088u at z=33. The webview's render loop was wedged (documented flake) so live motion was verified by driving the registered tick callbacks, not the rAF loop. Uncommitted with the rest of the batch.
- **2026-08 Session — board dead-zone fillers**: the board was ~60% empty substrate. Added eight recognizable, hoverable parts (components.js): three clickable tactile switches SW1-3 along the right edge, RF shield can RF1 (top-right, with embossed frame + vent slots), upright electrolytic C5 (bottom-center-right, gold top + polarity cross), pin header HDR1 (top-left, 6 gold pins), shielded inductor L1 (top-center, silver band + gold pads), and blue trimpot RV1 (left-mid, gold screw). All passive (no emissive/per-frame writes), all with glow colors + scope readouts in hover.js, all pushed to interactiveObjects (23 interactive total, 31 named objects). SW1-3 are clickable: hover.js routes SWITCH clicks to pressTactile (components.js) — the cap dips 0.055 and springs back (0.09s power2.in yoyo), plus the click blip (sound toggle). Press feedback is direct-input so it deliberately skips the reduced-motion gate (same posture as hover glow). Verified: typecheck/build/smoke green, raycast net now 153 poses across 31 unique components, live scene check found all 8 parts and the press dip+restore exact. Uncommitted.
- **2026-08 Session — tactile switch behaviors**: SW1-3 now do real work (previously pure dressing). hover.js gained the `switchHandler` plumbing (`setSwitchHandler`, SWITCH branch calls pressTactile + clickBlip + handler — same pattern as the buzzer). components.js hoisted `SWITCH_POS` to an export (single source for switch geometry). main.js wires the routing: SW1 → `togglePower()` (night bench), SW2 → `pulseBuzzer()` (horn), SW3 → `focusProject(nearestChip)` — nearest computed by board-local 2D distance from SWITCH_POS[2] against the `projectChips` registry (all reuse existing entry points; no new state; scroll stays primary). Verified: typecheck/build/smoke green; live clean-pass test toggled night-bench ON→OFF via SW1, buzzer ring visible flipped false→true on SW2, SW3 routing picked ML1 (rightmost chip = nearest to x=4.7); the panel didn't open in the wedged webview because `journeyReady` never set (zero ScrollTriggers — same env limitation as chip clicks, not a wiring defect). Uncommitted.
- **2026-08 Session — silkscreen designators for filler parts**: added C2 block in board.js's silkscreen texture (after the RN1 outline): outline box + 40px designator for SW1-3, RF1, C5, HDR1, L1, RV1. Board-local → canvas mapping derived from the mounting-hole comment (canvas 160/1888 x 160/3936 ↔ local ±4.64, ±6.92 → SX 186.2, SY 272.8, origin 1024/2048). Label offsets clear existing marks: SW labels left of the buttons (right edge), C5 above the can (bottom fab-text band), RF1/HDR1/L1/RV1 beside their parts. Verified live via texture pixel sampling: all 8 labels + 8 box strokes drew at the computed anchors (edge density 0.38-0.63, clean interiors), labels sit 24-100px clear of their parts, all inside canvas margins; typecheck/build/smoke green. Uncommitted.
- **2026-08 Session — hero alignment + hero-scale ambient** — the user's screenshot showed the board 19px above the panel and dead-static on the first screen. (1) `journey.js`: `alignHeroToPanel()` — one Newton step shifts the hero/contact pose (pos+look together, pure translation preserves the 3/4 angle) until the board center projects onto the panel's center line (panel is pinned top:84/bottom:24 → center = h/2+30; the gap flips sign with aspect, so it must be per-viewport — exported `getCameraConfigForStop` for the smoke test). (2) `main.js` tick: `distScale = min(1 + (cameraZ-4.2)/12, 3)` passed to `updateBoardParallax` + `updateBenchSweep` — at hero z≈33 the float (±0.16u) projected to only ~4px; scaled it reads ~12px, sweep widens 3×. (3) `board.js`: `updateHoverShadow` clamps h/breathe to ±1 so the scaled float can't push the shadow outside its bounds. (4) smoke test: phase A2 (scaled-amplitude bounds at distScale=3, shadow clamp, sweep widening) + phase A3 (hero pose projects board center onto panel center, <4px). Verified live at the real hero framing: board 379.5px vs panel center 380px (−0.48px), horizontal drift 0px; float span 12.3px on-screen at z=33.
- **2026-08 Session — silkscreen designators for BZ1/TP1/TP2/ANT1** — the last four unmarked interactive parts. Extended the `fillerParts` array in board.js's silkscreen canvas: BZ1 box+label below the piezo disc (clear of the QR block and bottom fab band), 50px boxes + labels for TP1 (above, clear of U1's outline) and TP2 (below, clear of RN1), and ANT1's box sized to enclose the full meander (it pokes 0.5u above its 1.0×1.0 bounds mesh) with the label below clear of U1's. Every components.js interactive component now carries a silkscreen designator. Verified live by pixel sampling the generated texture: all 4 labels drawn (0.18-0.28 density vs 0.00-0.03 empty bands), all 4 boxes fully stroked (ANT1's edges 44/44, 32/32 lit); typecheck/smoke/build green.
- **2026-08 Session — smooth scroll-snap layer** — the journey was a pure pulley (ScrollTrigger scrub followed the scrollbar 1:1; a wheel notch was a hard yank). Added a page-to-page layer in journey.js (registered once in initJourney, gated on journeyReady/reduced-motion/focus): (1) **Wheel hijack** (passive:false, deltaMode-normalized): deltas accumulate into section steps (120px per step); one notch = one section, glided with the site's unified 1.6s power2.inOut. Rapid input chains AT MOST 2 glides per burst — the queue counts the in-flight step (consumed in onComplete, not at start), so a trackpad flick can't chain 3+ (a first implementation decremented at pump-time and a 4-notch burst ran 3 glides; caught live and fixed). (2) **Settle-snap**: any other scroll (trackpad momentum, touch, keyboard, scrollbar) that rests between stops glides to the nearest section (280ms idle). (3) **Gates**: skipped under reduced motion (native scroll, no hijack), while a chip is focused (native scrub releases focus exactly as before), on ctrl/meta+wheel (browser zoom), and when wheeling inside a nested scrollable (the fixed datasheet panel scrolls itself — detected by walking ancestors for scrollHeight > clientHeight). (4) scrollToSection resets the snap state so a nav click mid-glide can't leave glideActive wedged (killed tweens don't fire onComplete). Verified live against the app's real journey: one notch at hero glides 0→840 over ~1.5s with a textbook power2.inOut curve (4px at 175ms, not a 120px pulley jump); a 4-notch burst chains exactly 2 glides and lands on 2520 and holds; parked at 3000 settle-snaps to 3360; reduced-motion and panel-wheel events leave defaultPrevented false; camera arrives at the exact stop pose (drift 0.000) for both far (contact) and close (experience) stops. typecheck/build/smoke green. Uncommitted.
- **2026-08 Session — scroll-snap tune: 1.2s glide + 3-step burst** — per the tuning request. Extracted `SECTION_TRANSITION_DURATION = 1.2` (shared by the wheel/snap glides AND scrollToSection nav, so every section change moves at one pace — one constant tunes the journey) and raised `MAX_QUEUED_STEPS` 2 → 3 (a flick now covers half the 6-section journey). Verified live: one notch lands on the next stop at ~1200ms with a clean power2.inOut curve (6px @150ms → 413 @590ms → 840 @1200ms); a 5-notch burst chains exactly 3 glides (840→1680→2520→3360) and holds; camera arrives at the exact stop pose (drift 0.000); no settle-snap misfire. The feel tradeoff: 1.2s keeps the slow-start/slow-end ease (the original "too fast" complaint was the pulley's abruptness, not duration) but the chained flow no longer drags — 3 chained glides now take 3.6s vs 4.8s at 1.6s. Revert is the single constant.
- **2026-08 Session — headless coverage for the snap layer** — extracted the two pure seams from the DOM-bound wheel path in journey.js and covered them in the smoke test (phase A4): `computeDirectionalStop(stops, y, dir)` (nearest stop beyond scroll with the 2px no-re-target tolerance, clamped to journey ends — the DOM wrapper `directionalStop` delegates with `getStopScrolls()` + `window.scrollY`) and `wheelStepQueue(delta, accum, stepPx, maxSteps)` (delta → capped ±max section steps with the sub-threshold remainder carried — `onJourneyWheel` now calls it, replacing the inline while loops; behavior verified identical live). A4 asserts: 11 direction cases (mid-leg both ways, exactly-on-stop both ways, 2px tolerance, hero/end clamping, empty stops), 8 accumulator cases (single/multi-step deltas, sub-threshold carry, negative, oversized capped with remainder kept), and 9 burst invariants via a simulation of the real queue path — N notches chain exactly min(N, MAX_QUEUED_STEPS) glides (1→1, 2→2, 3→3, 5→3, 9→3, backward, mixed-direction net, trackpad-style small-delta accumulation). typecheck/build/smoke green; live re-verified the refactored handler: one notch → 840 @ ~1.2s, 5-notch burst → 3360. Uncommitted.
- **2026-08 Session — CI trio (build + typecheck + smoke)** — extended `.github/workflows/smoke.yml` with a parallel `build-typecheck` job (`tsc --noEmit` + `vite build`, catching bundling/import errors) beside the existing `smoke` job; renamed the workflow display name smoke → `ci` (the badge URL is file-based so `smoke.yml/badge.svg` still resolves — the label just reads "ci - passing"). README updated: badge alt text → "CI status", the CI sentence now names the trio, and the file-map comment. YAML validated with the js-yaml CLI; the exact trio ran green locally. Uncommitted.
- **2026-08 Session — keyboard section-stepping** — ArrowDown/PageDown advance one section, ArrowUp/PageUp go back, glided with the same 1.2s SECTION_TRANSITION_DURATION as the wheel. journey.js: extracted the pure `stepQueue(queue, dir, cap)` clamp (exported for the smoke test) + a shared `queueStep(dir)` used by both the wheel handler and the new `onJourneyKeydown` (registered in wireSmoothScroll) — so keyboard chains through the same burst-capped queue as wheel input (holding the key keeps advancing, 3 at a time). Gates: skipped while the flying scope probe is active (arrows belong to the probe — gates on the `probe-flying` body class, avoiding a journey→probe import cycle), while typing in a field (INPUT/TEXTAREA/contentEditable), with modifiers (ctrl/alt/meta), under reduced motion, and pre-journey. Focus release matches the wheel: a step key while a chip is focused drops the focus view then steps. Smoke test phase A4 extended: 9 stepQueue unit cases (cap both directions, multi-step deltas, custom cap) + 7 keyboard burst invariants via a per-press stepQueue simulation (0/1/2/5/8 presses, custom cap, ArrowDown×2+ArrowUp net). Verified live: ArrowDown→840, PageDown→1680, ArrowUp→840 all at ~1.2s; 5 rapid ArrowDowns chain exactly 3 glides (3360); probe-flying and focused-input presses leave the page unmoved; focused chip + ArrowDown releases focus and lands on 1680. typecheck/build/smoke green.
- **2026-08 Session — scroll-follow fix + GPA 9.48 + hero self-description (committed)**: the snap layer auto-advanced pages on tiny input — one 120px mouse notch queued a full-section glide, and the 280ms settle-snap dragged any rested scroll to the nearest stop (a small scroll could pull the page a whole section "without even scrolling"). Reworked in `src/scroll/journey.js`: ① `WHEEL_STEP_PX` 120 → 240 — a section step now needs two standard notches (or one flick), so a single notch stays sub-threshold; ② sub-threshold deltas are no longer `preventDefault`-swallowed — the event falls through to NATIVE scroll, so the page follows the finger proportionally (mid-glide sub-threshold input is still swallowed so the browser can't fight the running tween, with the delta riding the accumulator for a possible chain); ③ **settle-snap removed entirely** (`onJourneyScroll` + `SNAP_SETTLE_MS` + the scroll listener deleted) — native scrolls now rest where they land, so nothing ever drags the page a full section on its own. Keyboard stepping and nav glides unchanged (same queue). Smoke phase A4 updated to the new semantics (single notch = 0 steps with carry, two notches = 1, burst sims re-derived: 9×120px → capped 3 glides). Also: CGPA corrected **9.51 → 9.48** in all five spots (index.html hero badge + About VREF row, portfolio.js stats/grade/timeline detail) and the hero claim rewritten as a first-person self-description ("I'm Parameshwaran S — an ECE + Data Science undergrad at SRM IST, Chennai, who builds for real..." — deployed ML APIs, live GPS tracking, 24-hour local AI agent, solo-founded flyrank.ai) mirrored in `heroLine`. Verified: typecheck/build/smoke green; live — hero text + 9.48 badges render, parking mid-leg at 400 holds (no snap), one synthetic notch moves nothing, two notches glide exactly to 840, ArrowDown still steps to 840, console clean.
- **2026-08 Session — portfolio fixation batch (locked copy, single-source stats, CTA unify, nav single-row, J1 framing, gems, skills regroup)**: (1) **Locked About bio** — the About panel body (index.html) + portfolio.js `bio` now carry the exact locked copy verbatim ("Hi, I'm Parameshwaran — a 3rd-year ECE student specializing in Data Science at SRM Ramapuram..."). (2) **Broken-mesh audit** — walked the whole scene graph + read the WebGL framebuffer at all six stops: no mesh is missing a material/texture (the big planes are the textured silkscreen/pour/backdrop; the gray slab the user saw was the board's bottom region at the Experience stop — the default camera looked at the dark bottom silkscreen band instead of J1). (3) **Experience camera** — new `CUSTOM_CAMERAS` override in journey.js: pos (0,-5.5,2.4), look (0,-6.2,0.17) frames J1's actual mesh (world y -6.205) dead-center at ~37% frame height, with the silkscreen band above; the ride-the-trace curve builder now lets the destination's own z win (was forced to the source's cruise z 4.2, which discarded the close framing). Verified by framebuffer: J1 projects to NDC (0, 0.01) and the center row is J1's body (orange = its 0xff8800 hover glow). (4) **Nav single-row** — `.hud-nav` flex-wrap nowrap (the five buttons can never wrap; the bar's own flex-wrap drops the legend/CTA to row 2 instead), explicit breakpoint ladder: probe/extras hints hide ≤1280, scope ≤1100, remaining hints ≤900. (5) **Truncation** — `.contact-secondary` flex-wrap (the "CH..." clipped location row now wraps); the "Email is the sec" sighting was the 16ms/char typewriter mid-flight (completes in ~3s) / the stale-dev-deps freeze, not a container bug. (6) **Single-source stats** — portfolio.js now defines `const GPA = '9.48/10'` (used in stats, education, timeline) and the hero badge row + About VREF are rendered from `personalInfo.stats` (sections.js renderHeroStats/renderAboutVref) — no duplicated GPA literals outside the data file. Four hero stats restored: GPA 9.48/10, PROJECTS 9, HACKATHONS 2, CERTS 7. (7) **CTA unify** — contact's "OPEN PRIMARY UPLINK →" → "CONNECT ON LINKEDIN →" (all four CTAs identical; the only mismatch). (8) **Hero wording** — the claim now reads the locked "I ship real, working projects — deployed ML APIs, live GPS tracking, a 24-hour fully-local AI calling agent, and flyrank.ai, my own startup, built solo from the ground up. Still figuring the rest out." (9) **Tier-2 component readouts** — verified every interactive component (31 named, 23 interactive) already has a SCOPE_MAP instrument reading; added `hover.getShortMeasurement(name)` and the cursor probe chip now shows "REF V · F" near the pointer (e.g. "J1 5V · 480Mbps") instead of the bare ref. (10) **Project gems** — each chip gets a category-colored infinity stone: AI/ML rose 0xf43f5e, FULL-STACK cyan 0x22d3ee, SYSTEMS blue 0x3b82f6 (the same hues the filter pins now use via `.proj-filter.active[data-filter=…]`); shipped = polished octahedron (metalness 0.7, rough 0.12, flatShading) sharing the steady LED's breathe; building = rough icosahedron (roughness 0.95) sharing the flicker driver; the filter dims them via dimmedLedMats; focusProject flashes gemMat (0.25→2.8) in sync with the LED so the gem lights up on click. (11) **Status markers** — `.proj-ds-status` is now a labeled chip (SOLDERED / BREADBOARD via ::after) with only the dot flickering (the label stays readable; whole-chip flicker would strobe it). (12) **Skills regroup** — C2 "DISPLAY & I/O BANK" → "COMM & INTERFACE BUS", C3 "STORAGE CONTROLLER BANK" → "DATA & ANALYTICS BANK" (Git moved out), Git → C4 "FIRMWARE, RF & TOOLING"; each pill carries data-weight (heavy ≥4 projects, medium 2–3) with gold/signal border glow and a hover-revealed "USED IN: CrowdPulse, Dialora, EcoMentor AI" proof line derived from the projects' tags. (13) **Nav naming decision: THEMATIC KEPT** ([CORE]/[MODULES]/[LIBRARY]/[EXPERIENCE]/[TX/RX]) with a plain-English sublabel (About/Projects/Skills/Experience/Contact) revealed on hover via a real span (both pseudo-slots are taken: ::before route line, ::after active pad), pointer:fine gated. Verified: typecheck + build + smoke green (207 materials incl. 9 gems, phase C raycast 159/159, phase B frozen); live DOM checks for every item; framebuffer scans of all six stops show no flat-gray slabs (≤3.3%). The preview webview's screenshot compositor stayed wedged this session (first two captures worked, then died) — DOM + accessibility + WebGL-readback verification used instead.
- **2026-08 Session — liveliness + minigame batch (Tier 0-3)**: (1) **Gray-slab root cause found + fixed** — re-audited the whole scene graph (every named + anonymous mesh, material, texture): NO mesh is missing a material/texture. The recurring "flat gray/black rectangle" was the camera framing: the Experience stop sat at z=2.4 with J1's featureless silver body at ~87% frame width, and U2's dark package read flat at close stops. Fixed at the source: **J1 detailed** (components.js — dark recessed port opening + 4 gold contact blades + metal mounting ears with screw-boss holes, so it reads as a USB-A jack at any zoom) and **U2 detailed** (canvas-textured package marking plane: pin-1 dot, U2 ref, lot line, gold inner seam — same pattern as U1's silicon die). **Experience camera reframed** (journey.js CUSTOM_CAMERAS → pos (0,-4.0,9.0) look (0,-5.2,0.08)): now frames the whole signal path — U1→RN1→BZ1→J1 + D1-D7 LEDs — J1 center-low, no dominant slab. (2) **Idle ambient confirmed live** — camera micro-drift after 3s stillness (0.0115u over 1.5s at component framing, eases back out on interaction), D1-D7 staggered seeded pulse (0.125→0.683), gold current dot traveling every route, copper ripple — all verified by driving the app's real tick callbacks. (3) **LCD1 — 2.4" display + SIGNAL SNAKE** (new `src/three/lcd.js`): bezel + trim + power LED + screen quad with a real 2D-canvas game pushed via THREE.CanvasTexture (needsUpdate only on change). The snake is a copper trace eating gold signal packets; red noise blocks end the run; "SIGNAL INTEGRITY: N" score; attract-mode demo snake plays itself at rest (fixed-seed LCG — deterministic, identical every load), static title under reduced motion; click LCD1 (Tier-1 raycast → camera glide via journey.focusLcdCamera, no DOM panel — the game IS the content) → player mode (arrows/WASD steer, Enter restarts, Esc exits). **Exclusive keyboard**: `body.lcd-active` while focused — gated in the probe listener, handleSectionKey (1-6), journey arrow stepping, Ctrl+K palette, and the cheat buffer; the game's own keydown registers once and prevents page scroll. Exit paths: Esc (glide back via setLcdExitHandler→clearFocus), re-click toggle, chip-click (focusProject exits LCD first), and **any scroll** — the focus-clear moved from setCameraAtT into the ScrollTrigger onUpdate so copper-ride legs release too (pre-existing gap: ride legs never cleared chip focus). Silkscreen `LCD1` label + outline added to board.js fillerParts; SCOPE_MAP entry (`LCD1 · 3.3V · 16MHz · GAME`); HUD extras hint now names all three capped extras: "WASD PROBE · P NIGHT BENCH · LCD1 GAME — OPTIONAL". (4) **Tier 2** — flyrank.ai's Experience "NOW" timeline entry now carries the Projects-card detail verbatim ("AI-powered ranking and visibility engine... Core build in active development; architecture and matching logic in progress — not publicly launched yet"). (5) **Tier 3 verified** — the gems from the last pass are live: 9 stones (7 polished octahedra shipped + 2 rough icosahedra breadboard), category hues match the filter pins, steady breathe vs flicker confirmed by sampling emissive over ticks. Verified: typecheck + build + smoke green (215 materials, phase C 159/159, phase B frozen); live on the app's real wiring — LCD click glides to (2.4,0.3,2.8), arrows/WASD/1-6 all gated while active, Esc/scroll restore keys (ArrowDown steps 3360→4200 after exit), framebuffer sweeps of all six stops show no slab (about 1% gray, skills 2%, projects 2.4% — component bodies only).
- **2026-08 Session — LCD headless coverage (smoke phase E)**: extended `tests/smoke-tick.mjs` to drive the SIGNAL SNAKE simulation headlessly through the same seam the browser tick uses, after refactoring `src/three/lcd.js` so the game LOGIC is separable from rendering: ① `stepLcdGame(delta)` — pure game clock (demo attract AI / player snake, death restart timer, STEP_SEC 0.15s cadence) extracted out of `updateLcdScreen`, so it runs even without a canvas context (the headless shim's null 2d context skips only the drawing); ② `lcdStateSnapshot()` — the serialized-state seam (score, snakeLen, head, dir, packets, noise, dead, demo, playerActive, FNV-1a gridHash) for assertions, same pattern as journey's stepQueue / idle's drift offset; ③ demo determinism — `resetGame(false)` re-seeds the LCG (seed 1234567) on EVERY demo reset, so every attract run incl. post-death reboots is byte-identical (player runs continue the stream so repeated plays differ). Phase E asserts: fresh-demo sanity (len 4, 1 packet, 2 noise), step cadence (8 ticks @1/60 no move, 9th steps once, no growth), determinism (two 60-tick runs deepStrictEqual), the passive gate (inactive ArrowUp passes through, zero preventDefault, trajectory untouched), the active gate (focused ArrowUp captured + queued → dir [0,-1] on the next step, playerActive), Esc release (class cleared, demo re-armed), and Enter reboot (unsteered run dies at the wall ~83 steps, Enter restarts player mode). The headless scene now also builds LCD1 (`createLcd` on the shim board), so the phase-C raycast sweep includes it: **33 components, 165/165** aimable poses. Verified: typecheck + build + smoke green. Live in the preview (app's real wiring): raycast-hover over LCD1 shows the Tier-2 readout (3.3V · 16MHz · GAME), canvas click engages `lcd-active` + camera glide, the game's own keydown preventDefaults ArrowUp while focused (confirmed via a bubble listener) and releases on Esc — the webview's canvas presents were unreliable for pixel-level screen reads this session (same compositor flakiness that wedged screenshots), so the visual step-by-step is covered headlessly + worth one eyeball pass in a real browser.
- **2026-08 Session — per-section ambient signatures**: every journey stop now has its own circuit-neighborhood feel. New `src/three/ambient-tunings.js` — a `SECTION_AMBIENT` map (one entry per section) where every field is a MULTIPLIER around the original 1.0 tuning, plus `getSectionAmbient(id)` (unknown ids fall back to the baseline so boot/lite mode change nothing). Signatures: hero = calm slow-breathing power-on presentation (ledFreq 0.7/ledAmp 0.5, ripple 0.8×/long waves, thin dust); about (U1 CPU core) = bright mid-tempo baseline; projects (U2 prototype zone) = fast pulse (1.35), tight short ripple (wavelength 0.75×), dense active dust (1.35 opacity) and fast pulses (1.4); skills (C1-C4 capacitor banks) = slow breathing (0.85), long calm waves, sparse dust; experience (J1 I/O port) = wide 1.55× wavelength waves, lively travel; contact (ANT1 RF) = fast travel (dot 1.35), wide waves, calm LEDs (0.55/0.45 — steady transmit), clear air (0.65 dust). Threaded through main.js's tick — `getActiveSectionId()` hoisted once per frame and passed into `updateLedArray` (tempo/brightness), `updateTraceRipple` (wave speed/wavelength/amp), `updateAmbientPulses` (travel speed, composed with the probe-energize surge multiplier), `updateTraceCurrent` (dot speed), `updateAmbientDust` (opacity=density + drift), `updateAmbientGoldFlecks` (same) — particles.js hoisted the shared dust/fleck materials to module scope so opacity writes per frame. Amplitude caps kept so the smoke bounds are mathematically guaranteed: ledAmp ≤ 1.0 (peak ≤ 0.7), rippleAmp ≤ 1.0 (≤ base+amp), drift ≤ 1.0 (drift-box bounds). Smoke **phase F**: asserts every section has a signature, all multipliers finite/non-negative with the caps, then sweeps each of the 6 sections 5s through the real update fns and reuses audit() (LED/ripple/dust/fleck/pulse bounds + no NaN) — green. Verified: typecheck/build/smoke all green; live — clean boot, no console errors during the Projects glide with the new wiring (webview screenshot compositor still flaky this session; the per-section behavior itself is pinned headlessly).
